diff --git a/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte
index 0ed4908..616be3d 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'add column after outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'add column after outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte.d.ts
index c35878b..a784e49 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AddColumnAfterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'add column after outline'
  * @prop ...restProps
  */
 declare const AddColumnAfterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte
index 7a49cb2..4673f93 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'add column before outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'add column before outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte.d.ts
index 67e2e9e..bd53fa6 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AddColumnBeforeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'add column before outline'
  * @prop ...restProps
  */
 declare const AddColumnBeforeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte
index 5f085cc..17d1357 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'address book outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'address book outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte.d.ts
index e708cf6..fddd5b6 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AddressBookOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'address book outline'
  * @prop ...restProps
  */
 declare const AddressBookOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte
index 662ebf7..2da74a3 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'address book solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'address book solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte.d.ts
index 5c59478..56e129b 100644
--- a/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AddressBookSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'address book solid'
  * @prop ...restProps
  */
 declare const AddressBookSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte
index 9eeefb6..19e1283 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'adjustments horizontal outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'adjustments horizontal outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte.d.ts
index 3ae3e58..784d30b 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'adjustments horizontal outline'
  * @prop ...restProps
  */
 declare const AdjustmentsHorizontalOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte
index 2a6fd4d..1c933c7 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'adjustments horizontal solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'adjustments horizontal solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte.d.ts
index 38fe521..a4f332f 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsHorizontalSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'adjustments horizontal solid'
  * @prop ...restProps
  */
 declare const AdjustmentsHorizontalSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte
index a60f426..4ec23bd 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'adjustments vertical outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'adjustments vertical outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte.d.ts
index d8e45f3..25b4979 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'adjustments vertical outline'
  * @prop ...restProps
  */
 declare const AdjustmentsVerticalOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte
index 81dfd50..90e4f27 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'adjustments vertical solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'adjustments vertical solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte.d.ts
index 424a695..094821b 100644
--- a/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AdjustmentsVerticalSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'adjustments vertical solid'
  * @prop ...restProps
  */
 declare const AdjustmentsVerticalSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte
index 6c19037..fe05160 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'align center outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'align center outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte.d.ts
index 059d829..042b894 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AlignCenterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'align center outline'
  * @prop ...restProps
  */
 declare const AlignCenterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte
index 9877a3a..39f84f1 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'align justify outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'align justify outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte.d.ts
index 59c20f9..a83498c 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AlignJustifyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'align justify outline'
  * @prop ...restProps
  */
 declare const AlignJustifyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte
index abdb45d..fa522fc 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'align left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'align left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte.d.ts
index 8bfd4bd..11adbe1 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AlignLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'align left outline'
  * @prop ...restProps
  */
 declare const AlignLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte
index 369765a..6af2fb2 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'align right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'align right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte.d.ts
index 7be28c1..75e1f3f 100644
--- a/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AlignRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'align right outline'
  * @prop ...restProps
  */
 declare const AlignRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte
index 58a8eab..4c49a85 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'angle down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'angle down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte.d.ts
index a63c857..1e839b9 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AngleDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'angle down outline'
  * @prop ...restProps
  */
 declare const AngleDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte
index 74a373d..b4a93fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'angle left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'angle left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte.d.ts
index 4ed44c4..13aea9d 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AngleLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'angle left outline'
  * @prop ...restProps
  */
 declare const AngleLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte
index f38d6b3..0c42efd 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'angle right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'angle right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte.d.ts
index edb7722..bc085ca 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AngleRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'angle right outline'
  * @prop ...restProps
  */
 declare const AngleRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte
index 0655769..e364378 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'angle up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'angle up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte.d.ts
index 5079f72..46924b8 100644
--- a/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AngleUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'angle up outline'
  * @prop ...restProps
  */
 declare const AngleUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte
index bc86b37..f34a1a5 100644
--- a/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'annotation outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'annotation outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte.d.ts
index 734c54c..dceabea 100644
--- a/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AnnotationOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'annotation outline'
  * @prop ...restProps
  */
 declare const AnnotationOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte
index 15309a9..803c28f 100644
--- a/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'annotation solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'annotation solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte.d.ts
index 2f1fd69..43c1f9d 100644
--- a/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AnnotationSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'annotation solid'
  * @prop ...restProps
  */
 declare const AnnotationSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte
index 49ea725..f01a8ed 100644
--- a/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'api key outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,9 +62,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'api key outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte.d.ts
index 21216dc..e2c5756 100644
--- a/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ApiKeyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'api key outline'
  * @prop ...restProps
  */
 declare const ApiKeyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte
index 14c5e88..ce525e3 100644
--- a/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'apple full outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'apple full outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte.d.ts
index 2a97c9c..6ad0f30 100644
--- a/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AppleFullOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'apple full outline'
  * @prop ...restProps
  */
 declare const AppleFullOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte
index 35cc43f..afe1779 100644
--- a/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'apple full solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'apple full solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte.d.ts
index eb83b16..feedb14 100644
--- a/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AppleFullSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'apple full solid'
  * @prop ...restProps
  */
 declare const AppleFullSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte
index 95fab85..2aeb6da 100644
--- a/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'apple solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'apple solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte.d.ts
index 1d6618e..5faf189 100644
--- a/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AppleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'apple solid'
  * @prop ...restProps
  */
 declare const AppleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte
index da1019a..78a80f6 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'archive arrow down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'archive arrow down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte.d.ts
index a3ca1d7..3e886a3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'archive arrow down outline'
  * @prop ...restProps
  */
 declare const ArchiveArrowDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte
index 68f1ce3..83ebaeb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'archive arrow down solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'archive arrow down solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte.d.ts
index 9ea57e4..f05a79b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveArrowDownSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'archive arrow down solid'
  * @prop ...restProps
  */
 declare const ArchiveArrowDownSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte
index ff701ca..5e23a1f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'archive outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'archive outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte.d.ts
index 587a11e..01c7aff 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'archive outline'
  * @prop ...restProps
  */
 declare const ArchiveOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte
index 4925c66..1963a0f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'archive solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'archive solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte.d.ts
index 723e6e1..7c61f5d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArchiveSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'archive solid'
  * @prop ...restProps
  */
 declare const ArchiveSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte
index 221f9cf..bcbe255 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte.d.ts
index d78dd7f..2aebfeb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow down outline'
  * @prop ...restProps
  */
 declare const ArrowDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte
index d3aefdd..593d716 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow down to bracket outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow down to bracket outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte.d.ts
index b3ecf27..0430e6c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowDownToBracketOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow down to bracket outline'
  * @prop ...restProps
  */
 declare const ArrowDownToBracketOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte
index 645b596..558e3f5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte.d.ts
index ee2094f..0846cf8 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow left outline'
  * @prop ...restProps
  */
 declare const ArrowLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte
index b51c9f7..e9245ef 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow left to bracket outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow left to bracket outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte.d.ts
index 22aa83e..48a65f6 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowLeftToBracketOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow left to bracket outline'
  * @prop ...restProps
  */
 declare const ArrowLeftToBracketOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte
index f00c118..75ae566 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow right alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow right alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte.d.ts
index e6e54f5..928ab6c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow right alt outline'
  * @prop ...restProps
  */
 declare const ArrowRightAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte
index 2de5588..8378e8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow right alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow right alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte.d.ts
index 22aa602..1c2d9d1 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow right alt solid'
  * @prop ...restProps
  */
 declare const ArrowRightAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte
index 3cf9998..2457528 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte.d.ts
index 21cdd6d..b53476a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow right outline'
  * @prop ...restProps
  */
 declare const ArrowRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte
index c237d0c..93e8d71 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow right to bracket outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow right to bracket outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte.d.ts
index 1cadfd6..814daa9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowRightToBracketOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow right to bracket outline'
  * @prop ...restProps
  */
 declare const ArrowRightToBracketOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte
index 5152cda..579dcdb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow sort letters outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow sort letters outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte.d.ts
index f6d5683..7b481d8 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowSortLettersOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow sort letters outline'
  * @prop ...restProps
  */
 declare const ArrowSortLettersOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte
index feb51d9..20cb3cc 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow up down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow up down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte.d.ts
index 7f209c5..0e58f9a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow up down outline'
  * @prop ...restProps
  */
 declare const ArrowUpDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte
index a4ceff2..6c9d252 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow up from bracket outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow up from bracket outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte.d.ts
index 4f2252c..403b45c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpFromBracketOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow up from bracket outline'
  * @prop ...restProps
  */
 declare const ArrowUpFromBracketOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte
index d40a660..781f802 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte.d.ts
index 376146d..c409306 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow up outline'
  * @prop ...restProps
  */
 declare const ArrowUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte
index f411a56..e078e2f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow up right down left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow up right down left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte.d.ts
index c8bacb0..12c2116 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightDownLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow up right down left outline'
  * @prop ...restProps
  */
 declare const ArrowUpRightDownLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte
index a644a9c..222a363 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow up right from square outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow up right from square outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte.d.ts
index dee6e48..ea109f5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow up right from square outline'
  * @prop ...restProps
  */
 declare const ArrowUpRightFromSquareOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte
index c31c192..8aa0e34 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrow up right from square solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrow up right from square solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte.d.ts
index 0ebacf2..ccdbc2a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowUpRightFromSquareSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrow up right from square solid'
  * @prop ...restProps
  */
 declare const ArrowUpRightFromSquareSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte
index cd7b511..97fab48 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrows repeat count outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrows repeat count outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte.d.ts
index 6a704a4..82fc07b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatCountOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrows repeat count outline'
  * @prop ...restProps
  */
 declare const ArrowsRepeatCountOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte
index 80981ab..ef007f0 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'arrows repeat outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'arrows repeat outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte.d.ts
index 0946fe5..411e370 100644
--- a/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ArrowsRepeatOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'arrows repeat outline'
  * @prop ...restProps
  */
 declare const ArrowsRepeatOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte
index 24f1eb5..b6783b7 100644
--- a/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'atom outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'atom outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte.d.ts
index 0594084..ab0278f 100644
--- a/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AtomOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'atom outline'
  * @prop ...restProps
  */
 declare const AtomOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte
index 225a6e3..a9e0808 100644
--- a/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'award outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'award outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte.d.ts
index b180f4a..cf54e2f 100644
--- a/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AwardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'award outline'
  * @prop ...restProps
  */
 declare const AwardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte
index 999b9b5..4bedef0 100644
--- a/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'award solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,6 +62,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'award solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte.d.ts
index 690c9bc..b9a9323 100644
--- a/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AwardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'award solid'
  * @prop ...restProps
  */
 declare const AwardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte b/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte
index f4441d7..6d2dec8 100644
--- a/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'aws solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'aws solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte.d.ts
index 925beb7..31b13a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/AwsSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'aws solid'
  * @prop ...restProps
  */
 declare const AwsSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte
index 6227f19..ea7e1d0 100644
--- a/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'backward step outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'backward step outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte.d.ts
index 155c115..f7d1aad 100644
--- a/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BackwardStepOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'backward step outline'
  * @prop ...restProps
  */
 declare const BackwardStepOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte
index 953a563..2bfaf05 100644
--- a/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'backward step solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'backward step solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte.d.ts
index 53a7ff5..a6d78d3 100644
--- a/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BackwardStepSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'backward step solid'
  * @prop ...restProps
  */
 declare const BackwardStepSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte
index 6d9b008..140daca 100644
--- a/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bacon outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,9 +61,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bacon outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte.d.ts
index 755d117..1a0fb25 100644
--- a/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BaconOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bacon outline'
  * @prop ...restProps
  */
 declare const BaconOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte
index 6948149..f9bb9e8 100644
--- a/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bacon solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bacon solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte.d.ts
index 0b71c9f..e999611 100644
--- a/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BaconSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bacon solid'
  * @prop ...restProps
  */
 declare const BaconSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte
index 12c3580..3fe5da2 100644
--- a/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'badge check outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'badge check outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte.d.ts
index 73378ff..65fcbbb 100644
--- a/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BadgeCheckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'badge check outline'
  * @prop ...restProps
  */
 declare const BadgeCheckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte
index 6855d58..327e1bd 100644
--- a/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'badge check solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'badge check solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte.d.ts
index 0572381..5500fd8 100644
--- a/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BadgeCheckSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'badge check solid'
  * @prop ...restProps
  */
 declare const BadgeCheckSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte
index 09bdd40..a24d45f 100644
--- a/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'ban outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'ban outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte.d.ts
index 3d685da..b212733 100644
--- a/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BanOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'ban outline'
  * @prop ...restProps
  */
 declare const BanOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte
index b1f7b0b..376b41d 100644
--- a/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'barcode outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -65,9 +64,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'barcode outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte.d.ts
index 67efbab..c59bf75 100644
--- a/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BarcodeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'barcode outline'
  * @prop ...restProps
  */
 declare const BarcodeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte
index 76fe1b7..8b3ece1 100644
--- a/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bars from left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bars from left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte.d.ts
index 9298a35..f72b020 100644
--- a/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BarsFromLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bars from left outline'
  * @prop ...restProps
  */
 declare const BarsFromLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte
index 8c8c644..c889ad8 100644
--- a/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bars outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bars outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte.d.ts
index 34202b3..672046e 100644
--- a/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BarsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bars outline'
  * @prop ...restProps
  */
 declare const BarsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte
index 8d59ad0..5b21995 100644
--- a/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'battery outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -66,9 +65,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'battery outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte.d.ts
index 6b0cb26..3dc1a77 100644
--- a/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BatteryOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'battery outline'
  * @prop ...restProps
  */
 declare const BatteryOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte b/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte
index eea9564..8977fca 100644
--- a/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'battery solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'battery solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte.d.ts
index 245f1b8..3b68321 100644
--- a/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BatterySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'battery solid'
  * @prop ...restProps
  */
 declare const BatterySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte
index 119fb9b..d3557a1 100644
--- a/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bed outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bed outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte.d.ts
index 9650860..28eab49 100644
--- a/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BedOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bed outline'
  * @prop ...restProps
  */
 declare const BedOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte
index c60e348..49a2ebf 100644
--- a/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bed solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bed solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte.d.ts
index 05f4767..fbd329d 100644
--- a/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BedSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bed solid'
  * @prop ...restProps
  */
 declare const BedSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte
index 8f090ba..a618788 100644
--- a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'beer mug empty outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'beer mug empty outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte.d.ts
index 27c0791..ee6f71f 100644
--- a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'beer mug empty outline'
  * @prop ...restProps
  */
 declare const BeerMugEmptyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte
index 4342df9..853a1c0 100644
--- a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'beer mug empty solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'beer mug empty solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte.d.ts
index a05e3d2..425a8a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BeerMugEmptySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'beer mug empty solid'
  * @prop ...restProps
  */
 declare const BeerMugEmptySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte
index c1b07b9..c86aabe 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell active alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell active alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte.d.ts
index cce67fd..51e75c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell active alt outline'
  * @prop ...restProps
  */
 declare const BellActiveAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte
index 865e6d3..dfb1396 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell active alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell active alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte.d.ts
index 60b25e0..92fccf8 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell active alt solid'
  * @prop ...restProps
  */
 declare const BellActiveAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte
index 59a39d4..b32ce02 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell active outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell active outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte.d.ts
index 141d35e..2c5d263 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell active outline'
  * @prop ...restProps
  */
 declare const BellActiveOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte
index 12e03e7..9ea1721 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell active solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell active solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte.d.ts
index 74f08e1..7f6fb49 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellActiveSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell active solid'
  * @prop ...restProps
  */
 declare const BellActiveSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte
index 063eac8..92f8138 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte.d.ts
index a94a979..5d7db0f 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell outline'
  * @prop ...restProps
  */
 declare const BellOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte
index 29439f0..59bf2e7 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell ring outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell ring outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte.d.ts
index 10b7a03..a6cdf09 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellRingOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell ring outline'
  * @prop ...restProps
  */
 declare const BellRingOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte
index 6a213f4..b0c9c51 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell ring solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell ring solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte.d.ts
index 366f924..9fbebc9 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellRingSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell ring solid'
  * @prop ...restProps
  */
 declare const BellRingSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte
index 7e91508..dbaa713 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bell solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bell solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte.d.ts
index 466e226..7a5d043 100644
--- a/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BellSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bell solid'
  * @prop ...restProps
  */
 declare const BellSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte
index 3cfcefc..109fc68 100644
--- a/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bitcoin solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bitcoin solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte.d.ts
index 955c141..2ff70e6 100644
--- a/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BitcoinSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bitcoin solid'
  * @prop ...restProps
  */
 declare const BitcoinSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte
index babfe6f..f5526c1 100644
--- a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'blender phone outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'blender phone outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte.d.ts
index 10debd0..1760171 100644
--- a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'blender phone outline'
  * @prop ...restProps
  */
 declare const BlenderPhoneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte
index b54ba6d..dec801c 100644
--- a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'blender phone solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'blender phone solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte.d.ts
index 98fdb87..7051e72 100644
--- a/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BlenderPhoneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'blender phone solid'
  * @prop ...restProps
  */
 declare const BlenderPhoneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte
index da593a3..89b2df4 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bone outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,9 +61,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bone outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte.d.ts
index f383f40..b18faaa 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BoneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bone outline'
  * @prop ...restProps
  */
 declare const BoneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte
index 103114e..82cc359 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bone solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bone solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte.d.ts
index d2754f4..280b281 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BoneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bone solid'
  * @prop ...restProps
  */
 declare const BoneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte
index ea53803..d018311 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'book open outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'book open outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte.d.ts
index 019b10c..91ea136 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'book open outline'
  * @prop ...restProps
  */
 declare const BookOpenOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte
index f61338a..e284e8c 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'book open reader outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'book open reader outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte.d.ts
index aba2da7..87c0c9f 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'book open reader outline'
  * @prop ...restProps
  */
 declare const BookOpenReaderOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte
index 94f3e76..69a27f0 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'book open reader solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'book open reader solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte.d.ts
index b80ee31..9beb4df 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenReaderSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'book open reader solid'
  * @prop ...restProps
  */
 declare const BookOpenReaderSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte
index a8a9620..a121fa7 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'book open solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'book open solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte.d.ts
index ce84410..48a4360 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookOpenSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'book open solid'
  * @prop ...restProps
  */
 declare const BookOpenSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte
index 8f4afad..b9e6bb5 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'book outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'book outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte.d.ts
index d16f678..34230b4 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'book outline'
  * @prop ...restProps
  */
 declare const BookOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte
index e0a6f9b..be653df 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'book solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'book solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte.d.ts
index 6f5f827..bcb0d48 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'book solid'
  * @prop ...restProps
  */
 declare const BookSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte
index 9d1c49a..a262e7c 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bookmark outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bookmark outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte.d.ts
index 4f98ff9..eb24242 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookmarkOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bookmark outline'
  * @prop ...restProps
  */
 declare const BookmarkOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte
index b36227f..9340199 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bookmark solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bookmark solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte.d.ts
index e22a0e9..809085d 100644
--- a/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BookmarkSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bookmark solid'
  * @prop ...restProps
  */
 declare const BookmarkSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte
index 5c4ae37..a5220ed 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'booth curtain outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'booth curtain outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte.d.ts
index ca4d9dc..b0ebc79 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BoothCurtainOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'booth curtain outline'
  * @prop ...restProps
  */
 declare const BoothCurtainOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte
index a3356b5..07cdd94 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'booth curtain solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'booth curtain solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte.d.ts
index eeb6f91..fb51f08 100644
--- a/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BoothCurtainSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'booth curtain solid'
  * @prop ...restProps
  */
 declare const BoothCurtainSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte
index c7a0de3..25eccfd 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bowl food outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bowl food outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte.d.ts
index 327ad72..f5f0e09 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BowlFoodOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bowl food outline'
  * @prop ...restProps
  */
 declare const BowlFoodOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte
index f68850a..08e8aa3 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bowl food solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bowl food solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte.d.ts
index 0c88d67..0dec0a7 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BowlFoodSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bowl food solid'
  * @prop ...restProps
  */
 declare const BowlFoodSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte
index 5dbeacc..c23a728 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bowl rice outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bowl rice outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte.d.ts
index dc432e4..bc904f9 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BowlRiceOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bowl rice outline'
  * @prop ...restProps
  */
 declare const BowlRiceOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte
index 3596e0c..61a1a73 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bowl rice solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bowl rice solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte.d.ts
index a43e7fd..f5f0d83 100644
--- a/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BowlRiceSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bowl rice solid'
  * @prop ...restProps
  */
 declare const BowlRiceSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte
index 2e628e5..ca60988 100644
--- a/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'brain outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'brain outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte.d.ts
index 95af2a1..f1ecb65 100644
--- a/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BrainOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'brain outline'
  * @prop ...restProps
  */
 declare const BrainOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte
index d79c9ee..090836b 100644
--- a/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'brain solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'brain solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte.d.ts
index 03cd76c..8efb1e0 100644
--- a/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BrainSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'brain solid'
  * @prop ...restProps
  */
 declare const BrainSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte
index b1c7b27..308424a 100644
--- a/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bread slice outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bread slice outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte.d.ts
index 6bf5d34..9c2af48 100644
--- a/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BreadSliceOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bread slice outline'
  * @prop ...restProps
  */
 declare const BreadSliceOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte
index 3de531f..b6dddb1 100644
--- a/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bread slice solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bread slice solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte.d.ts
index 8b6244f..b827eec 100644
--- a/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BreadSliceSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bread slice solid'
  * @prop ...restProps
  */
 declare const BreadSliceSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte
index 1275be4..1a22401 100644
--- a/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'briefcase outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'briefcase outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte.d.ts
index d057be0..6b3d462 100644
--- a/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BriefcaseOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'briefcase outline'
  * @prop ...restProps
  */
 declare const BriefcaseOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte
index 6ec537f..b7c8521 100644
--- a/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'briefcase solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'briefcase solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte.d.ts
index b3196b1..eb3ea37 100644
--- a/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BriefcaseSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'briefcase solid'
  * @prop ...restProps
  */
 declare const BriefcaseSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte
index 65c78f1..0caab8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bug outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bug outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte.d.ts
index 34ed1b6..106a34e 100644
--- a/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BugOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bug outline'
  * @prop ...restProps
  */
 declare const BugOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte
index 6fc3014..5a8fbb5 100644
--- a/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bug solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bug solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte.d.ts
index ed38167..e745d8a 100644
--- a/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BugSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bug solid'
  * @prop ...restProps
  */
 declare const BugSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte
index 777eee1..fe28a49 100644
--- a/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'building outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'building outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte.d.ts
index bfd3509..cb3874e 100644
--- a/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BuildingOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'building outline'
  * @prop ...restProps
  */
 declare const BuildingOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte
index 3068c49..63526ca 100644
--- a/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'building solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'building solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte.d.ts
index 1155191..7b7beeb 100644
--- a/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BuildingSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'building solid'
  * @prop ...restProps
  */
 declare const BuildingSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte
index 3071053..7869cda 100644
--- a/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bullhorn outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bullhorn outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte.d.ts
index de153bf..3b4180c 100644
--- a/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BullhornOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bullhorn outline'
  * @prop ...restProps
  */
 declare const BullhornOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte
index 1373eb5..6c38135 100644
--- a/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'bullhorn solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'bullhorn solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte.d.ts
index 6725eff..ee3b90d 100644
--- a/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BullhornSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'bullhorn solid'
  * @prop ...restProps
  */
 declare const BullhornSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte b/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte
index 673cc1c..5fd04b5 100644
--- a/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'burger outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'burger outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte.d.ts
index 54fade4..d54dd62 100644
--- a/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BurgerOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'burger outline'
  * @prop ...restProps
  */
 declare const BurgerOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte b/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte
index 9bcb581..471d464 100644
--- a/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'burger solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,6 +62,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'burger solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte.d.ts
index b4e7f65..8d8c521 100644
--- a/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/BurgerSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'burger solid'
  * @prop ...restProps
  */
 declare const BurgerSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte
index d4b5c72..526e258 100644
--- a/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cake candles outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cake candles outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte.d.ts
index f69d58b..595c5c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CakeCandlesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cake candles outline'
  * @prop ...restProps
  */
 declare const CakeCandlesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte
index 69d402b..2481e13 100644
--- a/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cake candles solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cake candles solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte.d.ts
index e7dac8b..6fbb3f0 100644
--- a/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CakeCandlesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cake candles solid'
  * @prop ...restProps
  */
 declare const CakeCandlesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte
index 58410f7..93e88c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar edit outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar edit outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte.d.ts
index 17748fd..2455b4c 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarEditOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar edit outline'
  * @prop ...restProps
  */
 declare const CalendarEditOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte
index aa2893f..285f21e 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar edit solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar edit solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte.d.ts
index 3fd9ff8..f86d739 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarEditSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar edit solid'
  * @prop ...restProps
  */
 declare const CalendarEditSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte
index 6037142..b1e68e3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar month outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar month outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte.d.ts
index 0ffa7dd..df1b452 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarMonthOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar month outline'
  * @prop ...restProps
  */
 declare const CalendarMonthOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte
index 0d9cfe6..04aa6a7 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar month solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar month solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte.d.ts
index 57bd80f..9b2ea61 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarMonthSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar month solid'
  * @prop ...restProps
  */
 declare const CalendarMonthSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte
index 26a012d..890c910 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,13 +19,12 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar plus outline',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -32,11 +32,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -58,6 +57,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte.d.ts
index 8c420f7..12c2da7 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarPlusOutline.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar plus outline'
  * @prop ...restProps
  */
 declare const CalendarPlusOutline: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte
index 79c1786..0a3a46e 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte.d.ts
index a897910..f290a29 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarPlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar plus solid'
  * @prop ...restProps
  */
 declare const CalendarPlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte
index ca59f6c..9cc7dbf 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar week outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar week outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte.d.ts
index 498389f..666be07 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarWeekOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar week outline'
  * @prop ...restProps
  */
 declare const CalendarWeekOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte
index eed3a06..3a9a2aa 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'calendar week solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'calendar week solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte.d.ts
index df83de0..7d35897 100644
--- a/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CalendarWeekSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'calendar week solid'
  * @prop ...restProps
  */
 declare const CalendarWeekSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte
index b64b217..c59e40e 100644
--- a/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'camera photo outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -66,9 +65,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'camera photo outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte.d.ts
index 000260e..d6eb9de 100644
--- a/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CameraPhotoOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'camera photo outline'
  * @prop ...restProps
  */
 declare const CameraPhotoOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte
index 7b4a600..6202317 100644
--- a/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'camera photo solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'camera photo solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte.d.ts
index 713bde1..36e80a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CameraPhotoSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'camera photo solid'
  * @prop ...restProps
  */
 declare const CameraPhotoSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte
index f9f45b4..48e33c6 100644
--- a/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'candy cane outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'candy cane outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte.d.ts
index 6382258..015ae08 100644
--- a/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CandyCaneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'candy cane outline'
  * @prop ...restProps
  */
 declare const CandyCaneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte
index df954e0..fae878d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'candy cane solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'candy cane solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte.d.ts
index 13a0fd0..7f978a5 100644
--- a/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CandyCaneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'candy cane solid'
  * @prop ...restProps
  */
 declare const CandyCaneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte
index f694cff..8621ae5 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caption outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caption outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte.d.ts
index b3cf104..b50d034 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaptionOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caption outline'
  * @prop ...restProps
  */
 declare const CaptionOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte
index 8edbcae..b5262f3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caption solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caption solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte.d.ts
index fac4b7a..4dc5574 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaptionSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caption solid'
  * @prop ...restProps
  */
 declare const CaptionSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte
index b0b149b..7eef6b2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte.d.ts
index 286bb78..ff22a14 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret down outline'
  * @prop ...restProps
  */
 declare const CaretDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte
index db66c49..8eb3cc1 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret down solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret down solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte.d.ts
index 2e32c5b..08be7c2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretDownSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret down solid'
  * @prop ...restProps
  */
 declare const CaretDownSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte
index 504c19e..6373df3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte.d.ts
index 933dbdf..39b94b1 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret left outline'
  * @prop ...restProps
  */
 declare const CaretLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte
index 82ceb86..a95e23a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret left solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret left solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte.d.ts
index 10deed0..2108cb8 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretLeftSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret left solid'
  * @prop ...restProps
  */
 declare const CaretLeftSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte
index 7c161b2..717a775 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte.d.ts
index 5f7357c..d374973 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret right outline'
  * @prop ...restProps
  */
 declare const CaretRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte
index 45f8f42..d3bfd25 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret right solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret right solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte.d.ts
index 14b99a9..d862a4c 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretRightSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret right solid'
  * @prop ...restProps
  */
 declare const CaretRightSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte
index 59f799b..fb1a30d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret sort outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret sort outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte.d.ts
index 6a78a2b..64bd373 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretSortOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret sort outline'
  * @prop ...restProps
  */
 declare const CaretSortOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte
index 944eb7e..2413f54 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret sort solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret sort solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte.d.ts
index 86e35ea..bd4b116 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretSortSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret sort solid'
  * @prop ...restProps
  */
 declare const CaretSortSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte
index 814a12d..54a29a8 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte.d.ts
index 548c770..4243138 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret up outline'
  * @prop ...restProps
  */
 declare const CaretUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte
index c4d2c53..24fc95e 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'caret up solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'caret up solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte.d.ts
index fe672aa..1a410aa 100644
--- a/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CaretUpSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'caret up solid'
  * @prop ...restProps
  */
 declare const CaretUpSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte
index 4f04dc2..0fd3b76 100644
--- a/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'carrot outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,9 +61,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'carrot outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte.d.ts
index 69e55b6..8a9d125 100644
--- a/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CarrotOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'carrot outline'
  * @prop ...restProps
  */
 declare const CarrotOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte
index 6b49092..51f5c3d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'carrot solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'carrot solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte.d.ts
index baeea65..4269a03 100644
--- a/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CarrotSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'carrot solid'
  * @prop ...restProps
  */
 declare const CarrotSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte
index 201eba4..6622369 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cart outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cart outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte.d.ts
index bb4959d..512f21d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CartOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cart outline'
  * @prop ...restProps
  */
 declare const CartOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte
index 07ec2ec..4a5cb0e 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cart plus alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cart plus alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte.d.ts
index 08c1069..2a9159d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cart plus alt outline'
  * @prop ...restProps
  */
 declare const CartPlusAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte
index 13de950..b242e0a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cart plus alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cart plus alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte.d.ts
index 976f93c..501d651 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cart plus alt solid'
  * @prop ...restProps
  */
 declare const CartPlusAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte
index f1f5c11..d755c08 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cart plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cart plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte.d.ts
index 72e3674..83f75d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cart plus outline'
  * @prop ...restProps
  */
 declare const CartPlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte
index 975b26a..8518506 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cart plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cart plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte.d.ts
index 5420cc6..023e833 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CartPlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cart plus solid'
  * @prop ...restProps
  */
 declare const CartPlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte
index 8224478..a83be6e 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cart solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cart solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte.d.ts
index 8018c5d..de2bbb4 100644
--- a/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CartSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cart solid'
  * @prop ...restProps
  */
 declare const CartSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte
index e557eb3..3f43f75 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cash outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cash outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte.d.ts
index ef34c41..f23d788 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CashOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cash outline'
  * @prop ...restProps
  */
 declare const CashOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte
index d48b213..66e79e7 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cash register outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cash register outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte.d.ts
index 6def9f9..33db4e5 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CashRegisterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cash register outline'
  * @prop ...restProps
  */
 declare const CashRegisterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte
index 86fab5a..6ccf909 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cash register solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cash register solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte.d.ts
index 6331947..9dcbb84 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CashRegisterSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cash register solid'
  * @prop ...restProps
  */
 declare const CashRegisterSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte
index 7cad974..0657ec6 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cash solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cash solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte.d.ts
index 711d23a..ef9880b 100644
--- a/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CashSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cash solid'
  * @prop ...restProps
  */
 declare const CashSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte
index 883e099..1e73a47 100644
--- a/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cell attributes outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cell attributes outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte.d.ts
index 52bbe1f..64d20bc 100644
--- a/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CellAttributesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cell attributes outline'
  * @prop ...restProps
  */
 declare const CellAttributesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte
index 6d5e88b..1d06d77 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chalkboard outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chalkboard outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte.d.ts
index 0a0fae2..9840558 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chalkboard outline'
  * @prop ...restProps
  */
 declare const ChalkboardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte
index 600f09c..5cd92b1 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chalkboard solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chalkboard solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte.d.ts
index 2abd049..16ff5ac 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chalkboard solid'
  * @prop ...restProps
  */
 declare const ChalkboardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte
index 6a1ee82..56448fc 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chalkboard user outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chalkboard user outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte.d.ts
index 82c7983..f8909f3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chalkboard user outline'
  * @prop ...restProps
  */
 declare const ChalkboardUserOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte
index 8d68e9f..70b8f1b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chalkboard user solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chalkboard user solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte.d.ts
index 4bb6751..ebd5433 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChalkboardUserSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chalkboard user solid'
  * @prop ...restProps
  */
 declare const ChalkboardUserSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte
index 473cb76..ec12e8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'champagne glasses outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'champagne glasses outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte.d.ts
index 11b1215..b34a01e 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'champagne glasses outline'
  * @prop ...restProps
  */
 declare const ChampagneGlassesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte
index 8d54e47..25a92fb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'champagne glasses solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'champagne glasses solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte.d.ts
index 7743b49..1533ac2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChampagneGlassesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'champagne glasses solid'
  * @prop ...restProps
  */
 declare const ChampagneGlassesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte
index f4bed28..ed959d1 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart line down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart line down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte.d.ts
index 2dd8251..7011f5f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartLineDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart line down outline'
  * @prop ...restProps
  */
 declare const ChartLineDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte
index acbf291..07ba76a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart line up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart line up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte.d.ts
index f67bff6..ec59625 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartLineUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart line up outline'
  * @prop ...restProps
  */
 declare const ChartLineUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte
index 7c1a71a..fcf00d8 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart mixed dollar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart mixed dollar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte.d.ts
index 2941e7a..818e39a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart mixed dollar outline'
  * @prop ...restProps
  */
 declare const ChartMixedDollarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte
index 067275b..5fbb943 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart mixed dollar solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart mixed dollar solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte.d.ts
index 9a1d686..4591f4a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartMixedDollarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart mixed dollar solid'
  * @prop ...restProps
  */
 declare const ChartMixedDollarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte
index 2eb7508..6e037ad 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart mixed outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart mixed outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte.d.ts
index 439b9dd..21fce45 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartMixedOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart mixed outline'
  * @prop ...restProps
  */
 declare const ChartMixedOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte
index bca4618..e8cda46 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte.d.ts
index c740657..10e8364 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart outline'
  * @prop ...restProps
  */
 declare const ChartOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte
index e096d38..28d2dbd 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart pie outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -68,9 +67,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart pie outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte.d.ts
index 8f77edb..137e2d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartPieOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart pie outline'
  * @prop ...restProps
  */
 declare const ChartPieOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte
index 374b34d..e163e53 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chart pie solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chart pie solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte.d.ts
index 5656aac..e9643b4 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChartPieSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chart pie solid'
  * @prop ...restProps
  */
 declare const ChartPieSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte
index a52c9f4..02cd30a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'check circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'check circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte.d.ts
index 8d4d79e..f6c90d7 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheckCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'check circle outline'
  * @prop ...restProps
  */
 declare const CheckCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte
index 7735819..1b99d4b 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'check circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'check circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte.d.ts
index 605684d..7560e85 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheckCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'check circle solid'
  * @prop ...restProps
  */
 declare const CheckCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte
index 44bf829..5a440ae 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'check outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'check outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte.d.ts
index 467a26a..5222bf0 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'check outline'
  * @prop ...restProps
  */
 declare const CheckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte
index d6adc0d..60a09d5 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'check plus circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'check plus circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte.d.ts
index 98a879a..552c3fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'check plus circle outline'
  * @prop ...restProps
  */
 declare const CheckPlusCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte
index ebfa6ad..e6d57ea 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'check plus circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'check plus circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte.d.ts
index 99bfbe1..24b81a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheckPlusCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'check plus circle solid'
  * @prop ...restProps
  */
 declare const CheckPlusCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte
index fd33170..ae775fc 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cheese outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cheese outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte.d.ts
index f40fc2f..57f14ff 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheeseOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cheese outline'
  * @prop ...restProps
  */
 declare const CheeseOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte
index 271bf8b..3a81e63 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cheese solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cheese solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte.d.ts
index 529d3d8..6a3903f 100644
--- a/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CheeseSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cheese solid'
  * @prop ...restProps
  */
 declare const CheeseSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte
index 2109729..06ce8e8 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron double down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron double down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte.d.ts
index 5b7bc43..44b2dac 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron double down outline'
  * @prop ...restProps
  */
 declare const ChevronDoubleDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte
index a44e5e0..dda9303 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron double left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron double left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte.d.ts
index 5cef6d7..09ad9a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron double left outline'
  * @prop ...restProps
  */
 declare const ChevronDoubleLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte
index 8a810b9..3cd3375 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron double right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron double right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte.d.ts
index 0d1c794..2c7bbab 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron double right outline'
  * @prop ...restProps
  */
 declare const ChevronDoubleRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte
index 4d1ebad..49f2575 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron double up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron double up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte.d.ts
index 3020005..c2e97b1 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDoubleUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron double up outline'
  * @prop ...restProps
  */
 declare const ChevronDoubleUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte
index 174146d..52ebba9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte.d.ts
index a54c765..cd75582 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron down outline'
  * @prop ...restProps
  */
 declare const ChevronDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte
index c4a6470..68cbe55 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron left outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron left outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte.d.ts
index 7d8c977..6c77684 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronLeftOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron left outline'
  * @prop ...restProps
  */
 declare const ChevronLeftOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte
index 61f65b8..44dadf9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte.d.ts
index 8dc464a..53d2334 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron right outline'
  * @prop ...restProps
  */
 declare const ChevronRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte
index 73ac15b..8145e8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron sort outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron sort outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte.d.ts
index 31b27c9..ea03eaa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronSortOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron sort outline'
  * @prop ...restProps
  */
 declare const ChevronSortOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte
index b762e97..848d6e9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'chevron up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'chevron up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte.d.ts
index 8f93531..a82e4f9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ChevronUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'chevron up outline'
  * @prop ...restProps
  */
 declare const ChevronUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte
index afda12d..034ef7a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'circle minus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'circle minus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte.d.ts
index f02825f..795b71f 100644
--- a/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CircleMinusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'circle minus outline'
  * @prop ...restProps
  */
 declare const CircleMinusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte
index 2bfc29d..7127c95 100644
--- a/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'circle minus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'circle minus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte.d.ts
index 0962509..ac700cd 100644
--- a/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CircleMinusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'circle minus solid'
  * @prop ...restProps
  */
 declare const CircleMinusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte
index 6b8ab6f..3deed5b 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'circle pause outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'circle pause outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte.d.ts
index 6bcd0e3..23b672b 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePauseOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'circle pause outline'
  * @prop ...restProps
  */
 declare const CirclePauseOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte
index b445365..7f85c7f 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'circle pause solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'circle pause solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte.d.ts
index 8c2ae8d..098063a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePauseSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'circle pause solid'
  * @prop ...restProps
  */
 declare const CirclePauseSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte
index 2e2c22c..b93a314 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'circle plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'circle plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte.d.ts
index edf98de..b26be4c 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'circle plus outline'
  * @prop ...restProps
  */
 declare const CirclePlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte
index 0a09497..746ac99 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'circle plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'circle plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte.d.ts
index 765b921..ec5771d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CirclePlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'circle plus solid'
  * @prop ...restProps
  */
 declare const CirclePlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte
index 3b4bdb5..986d8ff 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clapperboard play outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clapperboard play outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte.d.ts
index 281fb40..1b508b3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlayOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clapperboard play outline'
  * @prop ...restProps
  */
 declare const ClapperboardPlayOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte
index 9968126..01bb3f9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clapperboard play solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clapperboard play solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte.d.ts
index f66bd65..5c758fe 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClapperboardPlaySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clapperboard play solid'
  * @prop ...restProps
  */
 declare const ClapperboardPlaySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte
index 16dc917..bbc65b3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard check outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard check outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte.d.ts
index 242cb84..ef54093 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard check outline'
  * @prop ...restProps
  */
 declare const ClipboardCheckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte
index 22ecf56..2f398ba 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard check solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard check solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte.d.ts
index b9f291f..23aadb3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCheckSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard check solid'
  * @prop ...restProps
  */
 declare const ClipboardCheckSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte
index 235a731..bedeca2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard clean outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard clean outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte.d.ts
index e325d2a..304c9d9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard clean outline'
  * @prop ...restProps
  */
 declare const ClipboardCleanOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte
index 2e7cc21..06320eb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard clean solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard clean solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte.d.ts
index 89c47dc..0506ab9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardCleanSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard clean solid'
  * @prop ...restProps
  */
 declare const ClipboardCleanSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte
index 5f20382..bbc470c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard list outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard list outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte.d.ts
index 5f1a55d..f6bc410 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardListOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard list outline'
  * @prop ...restProps
  */
 declare const ClipboardListOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte
index f44382f..636fae2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard list solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard list solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte.d.ts
index 0a84a44..aadbcfb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardListSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard list solid'
  * @prop ...restProps
  */
 declare const ClipboardListSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte
index 4a7dfa6..b970ae6 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte.d.ts
index 783b375..71c1e0c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard outline'
  * @prop ...restProps
  */
 declare const ClipboardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte
index 62d8016..eecf88e 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clipboard solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clipboard solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte.d.ts
index 80e4715..1fc678b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClipboardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clipboard solid'
  * @prop ...restProps
  */
 declare const ClipboardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte
index ae1b132..6dc20fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clock arrow outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clock arrow outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte.d.ts
index b30e755..216efcf 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClockArrowOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clock arrow outline'
  * @prop ...restProps
  */
 declare const ClockArrowOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte
index 1c085bb..14f5853 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clock outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clock outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte.d.ts
index 2390f42..e958512 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClockOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clock outline'
  * @prop ...restProps
  */
 declare const ClockOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte
index adac43c..cda8124 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'clock solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'clock solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte.d.ts
index 1f90217..779c57b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ClockSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'clock solid'
  * @prop ...restProps
  */
 declare const ClockSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte
index e3f1df8..071b8b8 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte.d.ts
index 6933d65..a846726 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close circle outline'
  * @prop ...restProps
  */
 declare const CloseCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte
index e548bc1..ea5815f 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte.d.ts
index 3f0be96..ee6032a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close circle solid'
  * @prop ...restProps
  */
 declare const CloseCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte
index 3b3a2d0..6ccb8a9 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte.d.ts
index 18c674a..d1a3671 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close outline'
  * @prop ...restProps
  */
 declare const CloseOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte
index 538b298..9a4444f 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close sidebar alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close sidebar alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte.d.ts
index 8b2ac34..dc64955 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close sidebar alt outline'
  * @prop ...restProps
  */
 declare const CloseSidebarAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte
index 7187748..6ae34bd 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close sidebar alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close sidebar alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte.d.ts
index 7b085e4..0956c99 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close sidebar alt solid'
  * @prop ...restProps
  */
 declare const CloseSidebarAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte
index 654caad..f392622 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close sidebar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close sidebar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte.d.ts
index 53af9ec..b69d15c 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close sidebar outline'
  * @prop ...restProps
  */
 declare const CloseSidebarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte
index f58c423..5e8135a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'close sidebar solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'close sidebar solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte.d.ts
index 2785d56..94f7021 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloseSidebarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'close sidebar solid'
  * @prop ...restProps
  */
 declare const CloseSidebarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte
index a91861c..9938005 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cloud arrow up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cloud arrow up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte.d.ts
index 421459a..2cf7e83 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cloud arrow up outline'
  * @prop ...restProps
  */
 declare const CloudArrowUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte
index 9228822..a730106 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cloud arrow up solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cloud arrow up solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte.d.ts
index 3c2101c..dad5a84 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloudArrowUpSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cloud arrow up solid'
  * @prop ...restProps
  */
 declare const CloudArrowUpSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte
index 022a097..ddd94f2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cloud meatball outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cloud meatball outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte.d.ts
index f18e043..c8fdfc8 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloudMeatballOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cloud meatball outline'
  * @prop ...restProps
  */
 declare const CloudMeatballOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte
index 555299b..8239750 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cloud meatball solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -65,6 +64,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cloud meatball solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte.d.ts
index 04da5e8..f525b23 100644
--- a/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CloudMeatballSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cloud meatball solid'
  * @prop ...restProps
  */
 declare const CloudMeatballSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte
index 3d072e4..e0962b1 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code branch outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code branch outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte.d.ts
index 9a2c0cb..56d63fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeBranchOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code branch outline'
  * @prop ...restProps
  */
 declare const CodeBranchOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte
index 47a67a9..799b6a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code branch solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code branch solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte.d.ts
index 7c81fcb..cfc45e3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeBranchSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code branch solid'
  * @prop ...restProps
  */
 declare const CodeBranchSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte
index b5ad5bd..9cc87e1 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code fork outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code fork outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte.d.ts
index df2f87b..bfb5b8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeForkOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code fork outline'
  * @prop ...restProps
  */
 declare const CodeForkOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte
index e424e59..67a6fe0 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code fork solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code fork solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte.d.ts
index e0d7047..ca50dfc 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeForkSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code fork solid'
  * @prop ...restProps
  */
 declare const CodeForkSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte
index 0d78219..4f3bd73 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code merge outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code merge outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte.d.ts
index 2dd294a..928a449 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeMergeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code merge outline'
  * @prop ...restProps
  */
 declare const CodeMergeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte
index 0edc253..888f12c 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code merge solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code merge solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte.d.ts
index e9de520..20f17d7 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeMergeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code merge solid'
  * @prop ...restProps
  */
 declare const CodeMergeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte
index be7d054..7707cef 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte.d.ts
index c6bcae0..b015ec2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code outline'
  * @prop ...restProps
  */
 declare const CodeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte
index 84344a2..835c717 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code pull request outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code pull request outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte.d.ts
index 7fc8c97..1be1eca 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodePullRequestOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code pull request outline'
  * @prop ...restProps
  */
 declare const CodePullRequestOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte
index d23084e..d15df74 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'code pull request solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'code pull request solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte.d.ts
index f22dfe5..9e1ec5c 100644
--- a/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CodePullRequestSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'code pull request solid'
  * @prop ...restProps
  */
 declare const CodePullRequestSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte
index 9cb5c10..ace12f8 100644
--- a/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cog outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -68,9 +67,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cog outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte.d.ts
index c9b7db2..af271b3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CogOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cog outline'
  * @prop ...restProps
  */
 declare const CogOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte
index f236b81..257bc53 100644
--- a/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cog solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cog solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte.d.ts
index 8d062e1..e93c7c4 100644
--- a/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CogSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cog solid'
  * @prop ...restProps
  */
 declare const CogSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte
index 5a919dd..d393f91 100644
--- a/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'column outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'column outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte.d.ts
index 1687f94..3441229 100644
--- a/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ColumnOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'column outline'
  * @prop ...restProps
  */
 declare const ColumnOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte
index 465115e..e179dfd 100644
--- a/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'column solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'column solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte.d.ts
index 9e83144..7412373 100644
--- a/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ColumnSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'column solid'
  * @prop ...restProps
  */
 declare const ColumnSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte
index 36d6055..c082a1d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'command outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'command outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte.d.ts
index fa8ce99..63d0348 100644
--- a/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CommandOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'command outline'
  * @prop ...restProps
  */
 declare const CommandOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte
index 73a465b..cd48ca3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'compress outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'compress outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte.d.ts
index 50e3935..a207e69 100644
--- a/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CompressOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'compress outline'
  * @prop ...restProps
  */
 declare const CompressOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte
index 2cd7ff1..786202c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'computer speaker outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'computer speaker outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte.d.ts
index 0572c53..5cc6fff 100644
--- a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'computer speaker outline'
  * @prop ...restProps
  */
 declare const ComputerSpeakerOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte
index 4015d12..e53d3ec 100644
--- a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'computer speaker solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'computer speaker solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte.d.ts
index 8ad8f69..98b13cb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ComputerSpeakerSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'computer speaker solid'
  * @prop ...restProps
  */
 declare const ComputerSpeakerSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte
index 3146d4e..6864b74 100644
--- a/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cookie outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cookie outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte.d.ts
index 980ee09..d4982b3 100644
--- a/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CookieOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cookie outline'
  * @prop ...restProps
  */
 declare const CookieOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte
index caad338..7d45237 100644
--- a/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cookie solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cookie solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte.d.ts
index b12e06d..07355fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CookieSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cookie solid'
  * @prop ...restProps
  */
 declare const CookieSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte
index 9696ed4..a9cf322 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'credit card outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'credit card outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte.d.ts
index f35b398..6433437 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'credit card outline'
  * @prop ...restProps
  */
 declare const CreditCardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte
index 1e813d8..7a19375 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'credit card plus alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'credit card plus alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte.d.ts
index c39926d..431e4e5 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'credit card plus alt outline'
  * @prop ...restProps
  */
 declare const CreditCardPlusAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte
index 38c2654..f04b498 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'credit card plus alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'credit card plus alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte.d.ts
index 12156f0..a1c6748 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'credit card plus alt solid'
  * @prop ...restProps
  */
 declare const CreditCardPlusAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte
index 3b15a03..1202aea 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'credit card plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'credit card plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte.d.ts
index 1b774c7..0565abc 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'credit card plus outline'
  * @prop ...restProps
  */
 declare const CreditCardPlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte
index a6f6945..430e10b 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'credit card plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'credit card plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte.d.ts
index f9a0327..3499a32 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardPlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'credit card plus solid'
  * @prop ...restProps
  */
 declare const CreditCardPlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte
index 6ea9938..10c38d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'credit card solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'credit card solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte.d.ts
index f21d61d..26e99a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CreditCardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'credit card solid'
  * @prop ...restProps
  */
 declare const CreditCardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte
index 744d297..4fdae0a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'css solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'css solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte.d.ts
index 0253ea7..37bd181 100644
--- a/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CssSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'css solid'
  * @prop ...restProps
  */
 declare const CssSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte
index ca8d9db..aa2b0a6 100644
--- a/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'Cube solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'Cube solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte.d.ts
index 9aefb97..f44a26a 100644
--- a/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CubeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'Cube solid'
  * @prop ...restProps
  */
 declare const CubeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte b/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte
index 7eb3316..9e30ecc 100644
--- a/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cubes stacked outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cubes stacked outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte.d.ts
index 1cb9778..e7bbd2d 100644
--- a/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CubesStackedOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cubes stacked outline'
  * @prop ...restProps
  */
 declare const CubesStackedOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte b/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte
index ae1ff05..f74ed54 100644
--- a/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'cubes stacked solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'cubes stacked solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte.d.ts
index 0b27b09..7cc9543 100644
--- a/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/CubesStackedSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'cubes stacked solid'
  * @prop ...restProps
  */
 declare const CubesStackedSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte
index f5581e1..1b336ad 100644
--- a/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'database outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'database outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte.d.ts
index 097bdac..d344227 100644
--- a/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DatabaseOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'database outline'
  * @prop ...restProps
  */
 declare const DatabaseOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte
index f1fcb79..75b0e81 100644
--- a/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'database solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'database solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte.d.ts
index b4f3d89..b1840de 100644
--- a/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DatabaseSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'database solid'
  * @prop ...restProps
  */
 declare const DatabaseSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte
index f3c95a9..38b440a 100644
--- a/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'delete column outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'delete column outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte.d.ts
index 320a193..031bfb0 100644
--- a/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DeleteColumnOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'delete column outline'
  * @prop ...restProps
  */
 declare const DeleteColumnOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte
index e4188ca..7bd2f0c 100644
--- a/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'delete row outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'delete row outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte.d.ts
index baf9104..fba5c05 100644
--- a/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DeleteRowOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'delete row outline'
  * @prop ...restProps
  */
 declare const DeleteRowOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte
index eb1a81b..de4d852 100644
--- a/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'delete table outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'delete table outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte.d.ts
index cd5afbc..b50abe7 100644
--- a/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DeleteTableOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'delete table outline'
  * @prop ...restProps
  */
 declare const DeleteTableOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte
index 40e8a7a..8f70ade 100644
--- a/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'desktop pc outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'desktop pc outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte.d.ts
index 66b876d..103bfd4 100644
--- a/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DesktopPcOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'desktop pc outline'
  * @prop ...restProps
  */
 declare const DesktopPcOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte
index e5f9659..c53256a 100644
--- a/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'desktop pc solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'desktop pc solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte.d.ts
index dfab075..01afa80 100644
--- a/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DesktopPcSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'desktop pc solid'
  * @prop ...restProps
  */
 declare const DesktopPcSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte
index d117b28..17eef7c 100644
--- a/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'discord solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'discord solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte.d.ts
index c9a3b2b..6636ed1 100644
--- a/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DiscordSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'discord solid'
  * @prop ...restProps
  */
 declare const DiscordSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte
index de721eb..b4719d8 100644
--- a/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'dna outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'dna outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte.d.ts
index 395a49b..ee7da0c 100644
--- a/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DnaOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'dna outline'
  * @prop ...restProps
  */
 declare const DnaOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte
index 306a85f..116e384 100644
--- a/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'dollar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'dollar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte.d.ts
index 29d4add..3162e95 100644
--- a/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DollarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'dollar outline'
  * @prop ...restProps
  */
 declare const DollarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte
index e76d3ff..093052a 100644
--- a/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 3,
+		strokeWidth = ctx.strokeWidth || '3',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'dots horizontal outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 3
+@prop strokeWidth = ctx.strokeWidth || '3'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'dots horizontal outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte.d.ts
index c18a691..418b8b8 100644
--- a/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DotsHorizontalOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 3
+ * @prop strokeWidth = ctx.strokeWidth || '3'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'dots horizontal outline'
  * @prop ...restProps
  */
 declare const DotsHorizontalOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte
index 584c70f..6289265 100644
--- a/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 3,
+		strokeWidth = ctx.strokeWidth || '3',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'dots vertical outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 3
+@prop strokeWidth = ctx.strokeWidth || '3'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'dots vertical outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte.d.ts
index 279cffd..8d25a86 100644
--- a/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DotsVerticalOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 3
+ * @prop strokeWidth = ctx.strokeWidth || '3'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'dots vertical outline'
  * @prop ...restProps
  */
 declare const DotsVerticalOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte
index 5a425dc..2ed9337 100644
--- a/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'download outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'download outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte.d.ts
index 3e267cd..e6c6015 100644
--- a/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DownloadOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'download outline'
  * @prop ...restProps
  */
 declare const DownloadOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte
index f7ae56e..04d12cf 100644
--- a/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'download solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'download solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte.d.ts
index 847dde2..8b8ce86 100644
--- a/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DownloadSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'download solid'
  * @prop ...restProps
  */
 declare const DownloadSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte
index 7504d53..0e48893 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'draw square outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'draw square outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte.d.ts
index 5bb355d..74af46a 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DrawSquareOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'draw square outline'
  * @prop ...restProps
  */
 declare const DrawSquareOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte
index d0592c6..02320e4 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'draw square solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'draw square solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte.d.ts
index fbdac89..ddfce80 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DrawSquareSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'draw square solid'
  * @prop ...restProps
  */
 declare const DrawSquareSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte
index 3b1073f..1d87937 100644
--- a/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'dribbble solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'dribbble solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte.d.ts
index 90c38e3..eaeb8c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DribbbleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'dribbble solid'
  * @prop ...restProps
  */
 declare const DribbbleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte
index 98648e3..48ad021 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'dropbox solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'dropbox solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte.d.ts
index e90127b..7385a0d 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DropboxSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'dropbox solid'
  * @prop ...restProps
  */
 declare const DropboxSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte
index 5723ddd..5c98c63 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'droplet bottle alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'droplet bottle alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte.d.ts
index c265a23..7002e3e 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'droplet bottle alt outline'
  * @prop ...restProps
  */
 declare const DropletBottleAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte
index 4cab336..a4af1c9 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'droplet bottle alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'droplet bottle alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte.d.ts
index 94a2a49..7135454 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'droplet bottle alt solid'
  * @prop ...restProps
  */
 declare const DropletBottleAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte
index a39f9f7..7281d6c 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'droplet bottle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'droplet bottle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte.d.ts
index 756bb22..2782a35 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'droplet bottle outline'
  * @prop ...restProps
  */
 declare const DropletBottleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte
index fd1af6e..e31ccb7 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'droplet bottle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'droplet bottle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte.d.ts
index ade25e6..53ced57 100644
--- a/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DropletBottleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'droplet bottle solid'
  * @prop ...restProps
  */
 declare const DropletBottleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte
index 241b65d..7330556 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'drumstick bite outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'drumstick bite outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte.d.ts
index 96e12a4..6522bb0 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'drumstick bite outline'
  * @prop ...restProps
  */
 declare const DrumstickBiteOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte
index 3540246..73a8507 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'drumstick bite solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'drumstick bite solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte.d.ts
index f09b3cf..1a9e499 100644
--- a/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/DrumstickBiteSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'drumstick bite solid'
  * @prop ...restProps
  */
 declare const DrumstickBiteSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte
index 87b7700..94f9e6a 100644
--- a/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'edit outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'edit outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte.d.ts
index e353d0b..e5c0e19 100644
--- a/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EditOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'edit outline'
  * @prop ...restProps
  */
 declare const EditOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte b/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte
index f380c2a..29370ee 100644
--- a/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'edit solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'edit solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte.d.ts
index 19f854a..4729020 100644
--- a/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EditSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'edit solid'
  * @prop ...restProps
  */
 declare const EditSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte
index 3ae1518..0e3801a 100644
--- a/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'egg outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'egg outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte.d.ts
index d20414a..fc7e432 100644
--- a/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EggOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'egg outline'
  * @prop ...restProps
  */
 declare const EggOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte b/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte
index 9c20515..d6fa091 100644
--- a/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'egg solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'egg solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte.d.ts
index d99779a..7bcebbb 100644
--- a/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EggSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'egg solid'
  * @prop ...restProps
  */
 declare const EggSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte
index 0463192..47eed61 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'envelope open outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'envelope open outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte.d.ts
index 255a3fb..3e41078 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'envelope open outline'
  * @prop ...restProps
  */
 declare const EnvelopeOpenOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte
index 5d81f8b..ee7f708 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'envelope open solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -58,6 +57,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'envelope open solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte.d.ts
index ac6d09c..035b229 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeOpenSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'envelope open solid'
  * @prop ...restProps
  */
 declare const EnvelopeOpenSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte
index 440cd5b..095140f 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'envelope outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'envelope outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte.d.ts
index 2f4e44f..65be62f 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'envelope outline'
  * @prop ...restProps
  */
 declare const EnvelopeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte
index 460b6e4..e995f61 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'envelope solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'envelope solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte.d.ts
index 633eb7c..7b0adfc 100644
--- a/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EnvelopeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'envelope solid'
  * @prop ...restProps
  */
 declare const EnvelopeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte
index 3420521..4b54fc7 100644
--- a/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'euro outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'euro outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte.d.ts
index c6e5fd9..9fcdf0d 100644
--- a/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EuroOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'euro outline'
  * @prop ...restProps
  */
 declare const EuroOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte
index 2e107b5..47e4970 100644
--- a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'exclamation circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'exclamation circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte.d.ts
index 0a56c14..803cbd2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'exclamation circle outline'
  * @prop ...restProps
  */
 declare const ExclamationCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte
index 5456a9b..36bcce2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'exclamation circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'exclamation circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte.d.ts
index 5938298..b8b4113 100644
--- a/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ExclamationCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'exclamation circle solid'
  * @prop ...restProps
  */
 declare const ExclamationCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte
index b51f7f1..6d6a919 100644
--- a/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'expand outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'expand outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte.d.ts
index 579ada1..1636032 100644
--- a/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ExpandOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'expand outline'
  * @prop ...restProps
  */
 declare const ExpandOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte
index d830cb9..8c75af5 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'eye outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'eye outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte.d.ts
index 3e00bc2..1cf863a 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EyeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'eye outline'
  * @prop ...restProps
  */
 declare const EyeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte b/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte
index 3119acf..330d95a 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'eye slash outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'eye slash outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte.d.ts
index 9d218c6..62e8233 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EyeSlashOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'eye slash outline'
  * @prop ...restProps
  */
 declare const EyeSlashOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte b/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte
index ef3dc55..4c54081 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'eye slash solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,6 +62,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'eye slash solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte.d.ts
index f5ddea8..8905d22 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EyeSlashSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'eye slash solid'
  * @prop ...restProps
  */
 declare const EyeSlashSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte
index 6dcfc09..9639594 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'eye solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'eye solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte.d.ts
index 3098639..7626b8d 100644
--- a/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/EyeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'eye solid'
  * @prop ...restProps
  */
 declare const EyeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte
index d356f19..a90e864 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face explode outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,9 +61,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face explode outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte.d.ts
index d2900eb..7a90c9d 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceExplodeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face explode outline'
  * @prop ...restProps
  */
 declare const FaceExplodeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte
index 2b3e172..597a7f7 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face explode solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face explode solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte.d.ts
index edfd763..378cc0c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceExplodeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face explode solid'
  * @prop ...restProps
  */
 declare const FaceExplodeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte
index a444131..69c11ff 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face grin outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face grin outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte.d.ts
index 50b4f84..156b9b6 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face grin outline'
  * @prop ...restProps
  */
 declare const FaceGrinOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte
index 12f12d4..e445e35 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face grin solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face grin solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte.d.ts
index 1f841b2..20d58da 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face grin solid'
  * @prop ...restProps
  */
 declare const FaceGrinSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte
index b1dceb7..b18e0f0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face grin stars outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -77,9 +76,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face grin stars outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte.d.ts
index 0241211..70302e0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face grin stars outline'
  * @prop ...restProps
  */
 declare const FaceGrinStarsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte
index 7fd9b77..78812d8 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face grin stars solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face grin stars solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte.d.ts
index 5e70901..b43691d 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceGrinStarsSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face grin stars solid'
  * @prop ...restProps
  */
 declare const FaceGrinStarsSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte
index 6156d63..2f5c8ea 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face laugh outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face laugh outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte.d.ts
index 118c4d2..7761f4b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceLaughOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face laugh outline'
  * @prop ...restProps
  */
 declare const FaceLaughOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte
index d03fd9d..37e344b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'face laugh solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'face laugh solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte.d.ts
index dbeab47..8570525 100644
--- a/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FaceLaughSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'face laugh solid'
  * @prop ...restProps
  */
 declare const FaceLaughSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte
index d124f95..906a6d0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'facebook solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'facebook solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte.d.ts
index bad6170..a15cebe 100644
--- a/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FacebookSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'facebook solid'
  * @prop ...restProps
  */
 declare const FacebookSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte
index fd396cf..5d1bce7 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file chart bar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file chart bar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte.d.ts
index 7b16a7d..adca36c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileChartBarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file chart bar outline'
  * @prop ...restProps
  */
 declare const FileChartBarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte
index c3bc134..3aa0b8e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file chart bar solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file chart bar solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte.d.ts
index 8a7ce88..a672a32 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileChartBarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file chart bar solid'
  * @prop ...restProps
  */
 declare const FileChartBarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte
index 93656d4..ce0fbd2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file check outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file check outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte.d.ts
index 7fd5d25..8495aa8 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCheckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file check outline'
  * @prop ...restProps
  */
 declare const FileCheckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte
index a1b5722..0d4c329 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file check solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file check solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte.d.ts
index 5bd7392..3186ab9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCheckSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file check solid'
  * @prop ...restProps
  */
 declare const FileCheckSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte
index 2a22692..3bacc4a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file circle plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file circle plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte.d.ts
index 6cdc987..e39485e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file circle plus outline'
  * @prop ...restProps
  */
 declare const FileCirclePlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte
index 8232d52..b2f35f9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file circle plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file circle plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte.d.ts
index 6a6afd7..b844696 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCirclePlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file circle plus solid'
  * @prop ...restProps
  */
 declare const FileCirclePlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte
index 01d1030..1141b79 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file clone outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file clone outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte.d.ts
index 8cc36b9..01b3854 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCloneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file clone outline'
  * @prop ...restProps
  */
 declare const FileCloneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte
index 921f4d6..62a7635 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file clone solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file clone solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte.d.ts
index e96d731..9109981 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCloneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file clone solid'
  * @prop ...restProps
  */
 declare const FileCloneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte
index e704639..127a27c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file code outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file code outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte.d.ts
index a83c868..34a1056 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCodeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file code outline'
  * @prop ...restProps
  */
 declare const FileCodeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte
index 2a10212..ba54fda 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file code solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file code solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte.d.ts
index 7da54cd..f243927 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCodeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file code solid'
  * @prop ...restProps
  */
 declare const FileCodeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte
index e09c8e1..909510f 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file copy alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file copy alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte.d.ts
index 9fa2785..adbe423 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopyAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file copy alt outline'
  * @prop ...restProps
  */
 declare const FileCopyAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte
index da5412e..2a97a42 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file copy alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file copy alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte.d.ts
index a9d7fda..4e66a6e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopyAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file copy alt solid'
  * @prop ...restProps
  */
 declare const FileCopyAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte
index 9757d30..f90e3c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file copy outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file copy outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte.d.ts
index a330402..8df0332 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file copy outline'
  * @prop ...restProps
  */
 declare const FileCopyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte
index 10c6d0b..22bb3e3 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file copy solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file copy solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte.d.ts
index 12d6d11..5a54af1 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCopySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file copy solid'
  * @prop ...restProps
  */
 declare const FileCopySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte
index dc70e2e..080aca8 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file csv outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file csv outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte.d.ts
index a7ac5db..52f11d9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCsvOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file csv outline'
  * @prop ...restProps
  */
 declare const FileCsvOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte
index 98307e1..34d4138 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file csv solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file csv solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte.d.ts
index 24fe8c6..81a189e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileCsvSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file csv solid'
  * @prop ...restProps
  */
 declare const FileCsvSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte
index 06a1c46..def45df 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file doc outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file doc outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte.d.ts
index 50c39f9..cf2305a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileDocOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file doc outline'
  * @prop ...restProps
  */
 declare const FileDocOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte
index 4e5bb38..fe79620 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file doc solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file doc solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte.d.ts
index 8a871d1..ce652d4 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileDocSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file doc solid'
  * @prop ...restProps
  */
 declare const FileDocSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte
index ef0bd4e..26e9a63 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file export outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file export outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte.d.ts
index 3304f6d..fe4e8b9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileExportOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file export outline'
  * @prop ...restProps
  */
 declare const FileExportOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte
index 1457d8d..d792a50 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file export solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file export solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte.d.ts
index 927443a..a7e7d30 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileExportSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file export solid'
  * @prop ...restProps
  */
 declare const FileExportSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte
index bd85c0c..10cfc15 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file image outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,9 +61,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file image outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte.d.ts
index 439de92..c982577 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileImageOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file image outline'
  * @prop ...restProps
  */
 declare const FileImageOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte
index a11cca7..cbff1f8 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file image solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file image solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte.d.ts
index d73330b..be5139a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileImageSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file image solid'
  * @prop ...restProps
  */
 declare const FileImageSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte
index 30409d6..b7f85c0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file import outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file import outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte.d.ts
index cf2a50d..f318560 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileImportOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file import outline'
  * @prop ...restProps
  */
 declare const FileImportOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte
index aeab83b..dedef87 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file import solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file import solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte.d.ts
index 4076407..79c7afd 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileImportSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file import solid'
  * @prop ...restProps
  */
 declare const FileImportSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte
index 68d2595..9fe8dce 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file invoice outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file invoice outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte.d.ts
index 6c4dc9a..78959af 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileInvoiceOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file invoice outline'
  * @prop ...restProps
  */
 declare const FileInvoiceOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte
index b76d800..e2f85ad 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file invoice solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file invoice solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte.d.ts
index eefb842..1535143 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileInvoiceSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file invoice solid'
  * @prop ...restProps
  */
 declare const FileInvoiceSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte
index 4d50110..6b8deea 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file lines outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file lines outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte.d.ts
index dd5a720..c8f16d6 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileLinesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file lines outline'
  * @prop ...restProps
  */
 declare const FileLinesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte
index 457ac6d..8f3ca7b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file lines solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file lines solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte.d.ts
index 66ec9e6..13f20f2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileLinesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file lines solid'
  * @prop ...restProps
  */
 declare const FileLinesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte
index a44b3af..6c38685 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file music outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file music outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte.d.ts
index 7c890c9..6870fdc 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileMusicOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file music outline'
  * @prop ...restProps
  */
 declare const FileMusicOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte
index 0d3fa09..389fe4f 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file music solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file music solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte.d.ts
index 288f188..177289c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileMusicSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file music solid'
  * @prop ...restProps
  */
 declare const FileMusicSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte
index 4fa2d7e..65df7a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte.d.ts
index eae7ea2..3840ab9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file outline'
  * @prop ...restProps
  */
 declare const FileOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte
index 044a609..008b8a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file paste outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file paste outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte.d.ts
index 5cd5402..03d5d71 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePasteOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file paste outline'
  * @prop ...restProps
  */
 declare const FilePasteOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte
index a1cbdd3..34476d5 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file paste solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file paste solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte.d.ts
index 8590ec8..3c1b803 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePasteSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file paste solid'
  * @prop ...restProps
  */
 declare const FilePasteSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte
index 8b0d373..5a7e86d 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file pdf outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file pdf outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte.d.ts
index 057b889..59591dd 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePdfOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file pdf outline'
  * @prop ...restProps
  */
 declare const FilePdfOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte
index 99009ed..efa4e66 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file pdf solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file pdf solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte.d.ts
index 5d7798a..3638f16 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePdfSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file pdf solid'
  * @prop ...restProps
  */
 declare const FilePdfSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte
index 38202de..dd91246 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file pen outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file pen outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte.d.ts
index e4784fd..9b4ec90 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePenOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file pen outline'
  * @prop ...restProps
  */
 declare const FilePenOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte
index 3fb7cde..56ace03 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file pen solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file pen solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte.d.ts
index 56f2618..26ab708 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePenSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file pen solid'
  * @prop ...restProps
  */
 declare const FilePenSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte
index 8b0aef7..ccb6470 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file ppt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file ppt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte.d.ts
index 1e4a361..c61ce52 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePptOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file ppt outline'
  * @prop ...restProps
  */
 declare const FilePptOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte
index c07e5a2..18cfa2a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file ppt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file ppt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte.d.ts
index 601be70..dc16c96 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilePptSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file ppt solid'
  * @prop ...restProps
  */
 declare const FilePptSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte
index cffd2ac..618ae70 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file search outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file search outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte.d.ts
index 12dabb4..02c83be 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileSearchOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file search outline'
  * @prop ...restProps
  */
 declare const FileSearchOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte
index c730077..1624c66 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file search solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file search solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte.d.ts
index e71a844..97cdec0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileSearchSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file search solid'
  * @prop ...restProps
  */
 declare const FileSearchSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte
index 20518de..2d96626 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file shield outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file shield outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte.d.ts
index c711e82..f9d4b0b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileShieldOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file shield outline'
  * @prop ...restProps
  */
 declare const FileShieldOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte
index 95cba77..f6f8935 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file shield solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file shield solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte.d.ts
index 1d3cdaa..9373d2e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileShieldSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file shield solid'
  * @prop ...restProps
  */
 declare const FileShieldSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte
index 4a87bf4..e582ad7 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte.d.ts
index 7bbc609..7bef3f3 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file solid'
  * @prop ...restProps
  */
 declare const FileSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte
index cb3d2d9..1caa46a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file video outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file video outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte.d.ts
index 153eaac..26f0ae9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileVideoOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file video outline'
  * @prop ...restProps
  */
 declare const FileVideoOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte
index 3cb9d67..dc39713 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file video solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file video solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte.d.ts
index ba60d37..d76e75a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileVideoSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file video solid'
  * @prop ...restProps
  */
 declare const FileVideoSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte
index 617f00e..b5c3b62 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file word outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file word outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte.d.ts
index c77b839..a1fa4bf 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileWordOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file word outline'
  * @prop ...restProps
  */
 declare const FileWordOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte
index 60c91da..1fa7016 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file word solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file word solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte.d.ts
index 207750c..693053a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileWordSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file word solid'
  * @prop ...restProps
  */
 declare const FileWordSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte
index 89ff452..2f373ea 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file zip outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file zip outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte.d.ts
index 0840a84..5416ad8 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileZipOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file zip outline'
  * @prop ...restProps
  */
 declare const FileZipOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte
index bfeb37d..ae6dc4e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'file zip solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'file zip solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte.d.ts
index 1f90005..14ad7ac 100644
--- a/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FileZipSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'file zip solid'
  * @prop ...restProps
  */
 declare const FileZipSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte
index d8a7abe..02ae3c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'filter dollar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'filter dollar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte.d.ts
index 148f22b..fcc7ef2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilterDollarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'filter dollar outline'
  * @prop ...restProps
  */
 declare const FilterDollarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte
index 0908dc8..2755304 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'filter dollar solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'filter dollar solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte.d.ts
index ba4758b..6d68c83 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilterDollarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'filter dollar solid'
  * @prop ...restProps
  */
 declare const FilterDollarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte
index 047c6f5..dd81bdf 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'filter outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'filter outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte.d.ts
index 5e1d901..fc0cfb2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'filter outline'
  * @prop ...restProps
  */
 declare const FilterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte
index a1a2925..016bd35 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'filter solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'filter solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte.d.ts
index daedff1..c1deac8 100644
--- a/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FilterSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'filter solid'
  * @prop ...restProps
  */
 declare const FilterSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte
index 4f981d5..58d399b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fingerprint outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fingerprint outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte.d.ts
index 43e6016..5c3e9af 100644
--- a/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FingerprintOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fingerprint outline'
  * @prop ...restProps
  */
 declare const FingerprintOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte
index 29d4cc5..c787de9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fire outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fire outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte.d.ts
index 02a9f98..6820397 100644
--- a/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FireOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fire outline'
  * @prop ...restProps
  */
 declare const FireOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte
index dc3c009..118347b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fire solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fire solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte.d.ts
index 05a80dd..1a059ea 100644
--- a/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FireSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fire solid'
  * @prop ...restProps
  */
 declare const FireSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte
index e2f9601..b5019d1 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fish alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fish alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte.d.ts
index 947cbb0..94fde82 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FishAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fish alt outline'
  * @prop ...restProps
  */
 declare const FishAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte
index babf5af..7fa664a 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fish alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fish alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte.d.ts
index bba9f9d..83c2c0c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FishAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fish alt solid'
  * @prop ...restProps
  */
 declare const FishAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte
index 45debb5..ce6c4d9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fish outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fish outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte.d.ts
index f4af64f..c175605 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FishOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fish outline'
  * @prop ...restProps
  */
 declare const FishOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte
index c30c94a..ad82eb3 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fish solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fish solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte.d.ts
index 8aeb51f..29fb8f2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FishSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fish solid'
  * @prop ...restProps
  */
 declare const FishSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte
index 03e5b2b..7382631 100644
--- a/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'fix tables outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'fix tables outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte.d.ts
index 3dda072..c5d5e69 100644
--- a/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FixTablesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'fix tables outline'
  * @prop ...restProps
  */
 declare const FixTablesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte
index a4b673b..e762752 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'flag outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'flag outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte.d.ts
index 2669900..741131c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FlagOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'flag outline'
  * @prop ...restProps
  */
 declare const FlagOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte
index 4762296..e9107bc 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'flag solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'flag solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte.d.ts
index 94efd16..541ee9b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FlagSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'flag solid'
  * @prop ...restProps
  */
 declare const FlagSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte
index 622a024..dc91a35 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'flask outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'flask outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte.d.ts
index d3b4f20..4e2f046 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FlaskOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'flask outline'
  * @prop ...restProps
  */
 declare const FlaskOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte
index 27f2349..8124ec0 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'flask solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'flask solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte.d.ts
index 8d1c09a..4705f8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FlaskSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'flask solid'
  * @prop ...restProps
  */
 declare const FlaskSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte
index f9da230..74295d5 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'floppy disk alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'floppy disk alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte.d.ts
index f00a89b..e3146cc 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'floppy disk alt outline'
  * @prop ...restProps
  */
 declare const FloppyDiskAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte
index fdf9bd9..30d3bda 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'floppy disk alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'floppy disk alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte.d.ts
index 1715220..b893c0b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'floppy disk alt solid'
  * @prop ...restProps
  */
 declare const FloppyDiskAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte
index 29b240b..17bf3b4 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'floppy disk outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -66,9 +65,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'floppy disk outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte.d.ts
index 4dc7bb5..8058a20 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'floppy disk outline'
  * @prop ...restProps
  */
 declare const FloppyDiskOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte
index d9c379f..afb3d38 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'floppy disk solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'floppy disk solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte.d.ts
index 3cdff7e..f909888 100644
--- a/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FloppyDiskSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'floppy disk solid'
  * @prop ...restProps
  */
 declare const FloppyDiskSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte
index bdb9953..e6bf739 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'flowbite solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -66,6 +65,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'flowbite solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte.d.ts
index e4a83a9..902ab4f 100644
--- a/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FlowbiteSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'flowbite solid'
  * @prop ...restProps
  */
 declare const FlowbiteSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte
index ba2627e..5483edc 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder arrow right outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder arrow right outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte.d.ts
index 7038c05..41af22e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder arrow right outline'
  * @prop ...restProps
  */
 declare const FolderArrowRightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte
index c9a9020..74468ee 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder arrow right solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder arrow right solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte.d.ts
index f20d08c..11c9d2f 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderArrowRightSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder arrow right solid'
  * @prop ...restProps
  */
 declare const FolderArrowRightSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte
index e2c25f4..0cb9839 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder duplicate outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder duplicate outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte.d.ts
index 062e667..1f579a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder duplicate outline'
  * @prop ...restProps
  */
 declare const FolderDuplicateOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte
index 836a166..c919fed 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder duplicate solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder duplicate solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte.d.ts
index 3a586cb..9b3a85e 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderDuplicateSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder duplicate solid'
  * @prop ...restProps
  */
 declare const FolderDuplicateSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte
index 5585761..5735437 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder open outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder open outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte.d.ts
index e8a6fac..63a9631 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderOpenOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder open outline'
  * @prop ...restProps
  */
 declare const FolderOpenOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte
index ac75636..1d83194 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder open solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder open solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte.d.ts
index 7491b2d..b541534 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderOpenSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder open solid'
  * @prop ...restProps
  */
 declare const FolderOpenSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte
index fd89652..b99f36d 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte.d.ts
index b305bff..9b2dec5 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder outline'
  * @prop ...restProps
  */
 declare const FolderOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte
index 43df27c..6349259 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte.d.ts
index 6fcbd62..0b47270 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderPlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder plus outline'
  * @prop ...restProps
  */
 declare const FolderPlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte
index 28fd4ee..9ea6432 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte.d.ts
index 2e37ad3..74ea77c 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderPlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder plus solid'
  * @prop ...restProps
  */
 declare const FolderPlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte
index 0c157a1..ca1d4b9 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'folder solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'folder solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte.d.ts
index 6924a69..0f8842b 100644
--- a/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FolderSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'folder solid'
  * @prop ...restProps
  */
 declare const FolderSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte
index 6919327..44e7818 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'font color alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'font color alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte.d.ts
index 08f352d..907e698 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FontColorAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'font color alt solid'
  * @prop ...restProps
  */
 declare const FontColorAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte
index 953dbd9..64e43a1 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'font color outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'font color outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte.d.ts
index c73c727..e74ebaf 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FontColorOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'font color outline'
  * @prop ...restProps
  */
 declare const FontColorOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte
index 93b6630..252afd4 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'font family outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'font family outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte.d.ts
index c5d83b3..2a1dbc4 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FontFamilyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'font family outline'
  * @prop ...restProps
  */
 declare const FontFamilyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte b/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte
index 6c3a44f..69ec2de 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'font highlight outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'font highlight outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte.d.ts
index bf590b3..f3e4749 100644
--- a/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/FontHighlightOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'font highlight outline'
  * @prop ...restProps
  */
 declare const FontHighlightOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte
index 1e0bf41..01bd6b5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'forward outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'forward outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte.d.ts
index c386e75..b491cf7 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'forward outline'
  * @prop ...restProps
  */
 declare const ForwardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte
index 4a6f082..db3d2de 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'forward solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'forward solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte.d.ts
index b1ad743..1ea2eba 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'forward solid'
  * @prop ...restProps
  */
 declare const ForwardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte
index a1ea969..fd8bcbb 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'forward step outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'forward step outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte.d.ts
index 7684c83..a72cd4a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardStepOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'forward step outline'
  * @prop ...restProps
  */
 declare const ForwardStepOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte
index 4fcd2c4..4541364 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'forward step solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'forward step solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte.d.ts
index 95ba81b..087437d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ForwardStepSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'forward step solid'
  * @prop ...restProps
  */
 declare const ForwardStepSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte
index 2416b8c..280d8b1 100644
--- a/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'gift box outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'gift box outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte.d.ts
index fa5b896..0f9f6f2 100644
--- a/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GiftBoxOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'gift box outline'
  * @prop ...restProps
  */
 declare const GiftBoxOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte
index abe2214..ad35f12 100644
--- a/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'gift box solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'gift box solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte.d.ts
index c1f6342..6aaf27a 100644
--- a/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GiftBoxSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'gift box solid'
  * @prop ...restProps
  */
 declare const GiftBoxSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte
index 96b768d..d05c2be 100644
--- a/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'github solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'github solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte.d.ts
index cb6f646..4a41f8f 100644
--- a/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GithubSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'github solid'
  * @prop ...restProps
  */
 declare const GithubSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte
index 4deaf90..1eb2b94 100644
--- a/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'gitlab solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'gitlab solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte.d.ts
index 58be225..545cf16 100644
--- a/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GitlabSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'gitlab solid'
  * @prop ...restProps
  */
 declare const GitlabSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte
index 1fece8e..57dedef 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'glass water droplet outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'glass water droplet outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte.d.ts
index b1b10e1..f9ea3f3 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'glass water droplet outline'
  * @prop ...restProps
  */
 declare const GlassWaterDropletOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte
index b01e133..5d6a7ac 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'glass water droplet solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'glass water droplet solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte.d.ts
index a1b0eb6..037952b 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterDropletSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'glass water droplet solid'
  * @prop ...restProps
  */
 declare const GlassWaterDropletSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte
index 0b7e273..2be9d76 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'glass water outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'glass water outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte.d.ts
index 83d7ff4..cde072c 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'glass water outline'
  * @prop ...restProps
  */
 declare const GlassWaterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte
index 2842412..5bb4ef1 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'glass water solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'glass water solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte.d.ts
index dcd0da5..4eb02da 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GlassWaterSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'glass water solid'
  * @prop ...restProps
  */
 declare const GlassWaterSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte
index 7de5aea..cde1078 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'globe outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'globe outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte.d.ts
index ab3716d..d0d89e2 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GlobeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'globe outline'
  * @prop ...restProps
  */
 declare const GlobeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte
index ed4712b..6367619 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'globe solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'globe solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte.d.ts
index 737acfa..0b514a3 100644
--- a/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GlobeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'globe solid'
  * @prop ...restProps
  */
 declare const GlobeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte
index 4dbf89d..1aad48a 100644
--- a/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'go to next cell outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'go to next cell outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte.d.ts
index cef0c94..248d7ee 100644
--- a/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GoToNextCellOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'go to next cell outline'
  * @prop ...restProps
  */
 declare const GoToNextCellOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte
index b3ae529..19031e5 100644
--- a/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'go to prev cell outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'go to prev cell outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte.d.ts
index 8279c84..ef13a72 100644
--- a/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GoToPrevCellOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'go to prev cell outline'
  * @prop ...restProps
  */
 declare const GoToPrevCellOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte
index 127b392..e65970e 100644
--- a/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'google solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'google solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte.d.ts
index ad7754e..dac95cd 100644
--- a/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GoogleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'google solid'
  * @prop ...restProps
  */
 declare const GoogleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte
index db3b31d..0f6c9cc 100644
--- a/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'graduation cap outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'graduation cap outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte.d.ts
index 3f87f9b..68c6dc6 100644
--- a/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GraduationCapOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'graduation cap outline'
  * @prop ...restProps
  */
 declare const GraduationCapOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte
index 66bda14..17e5604 100644
--- a/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'graduation cap solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'graduation cap solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte.d.ts
index f6fd091..ed1a454 100644
--- a/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GraduationCapSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'graduation cap solid'
  * @prop ...restProps
  */
 declare const GraduationCapSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte
index e8ab158..12b38fe 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'grid outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'grid outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte.d.ts
index 0fca48a..3c89293 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GridOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'grid outline'
  * @prop ...restProps
  */
 declare const GridOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte
index 704a598..e3208a6 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'grid plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'grid plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte.d.ts
index 4f17b41..b2eac17 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GridPlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'grid plus outline'
  * @prop ...restProps
  */
 declare const GridPlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte
index 99b3597..3a50eac 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'grid plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'grid plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte.d.ts
index a6465eb..6e38dcd 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GridPlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'grid plus solid'
  * @prop ...restProps
  */
 declare const GridPlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte b/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte
index f662e8e..414a33b 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'grid solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'grid solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte.d.ts
index 83b2573..25b2b44 100644
--- a/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/GridSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'grid solid'
  * @prop ...restProps
  */
 declare const GridSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte
index c1d1922..7c74938 100644
--- a/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'hammer outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -66,9 +65,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'hammer outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte.d.ts
index 987dfa5..e133009 100644
--- a/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HammerOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'hammer outline'
  * @prop ...restProps
  */
 declare const HammerOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte
index 866bc6c..7070f81 100644
--- a/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'hammer solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'hammer solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte.d.ts
index ece2ae4..277f728 100644
--- a/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HammerSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'hammer solid'
  * @prop ...restProps
  */
 declare const HammerSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte
index 77fe86b..4844c8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'headphones outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'headphones outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte.d.ts
index 568b6f9..1ab5239 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HeadphonesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'headphones outline'
  * @prop ...restProps
  */
 declare const HeadphonesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte
index 30d4da7..08d30f1 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'headphones solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'headphones solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte.d.ts
index cb1b12e..53b8229 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HeadphonesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'headphones solid'
  * @prop ...restProps
  */
 declare const HeadphonesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte
index 122d2dc..c8f479e 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'heart outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'heart outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte.d.ts
index afd3698..7c4cb21 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HeartOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'heart outline'
  * @prop ...restProps
  */
 declare const HeartOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte
index 9fec3cb..bd843f8 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'heart solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'heart solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte.d.ts
index 3d8d885..87cfc1e 100644
--- a/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HeartSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'heart solid'
  * @prop ...restProps
  */
 declare const HeartSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte
index 93230e7..fba6f9d 100644
--- a/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'home outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'home outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte.d.ts
index 905d24c..c81929d 100644
--- a/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HomeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'home outline'
  * @prop ...restProps
  */
 declare const HomeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte
index 447ce70..a10692c 100644
--- a/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'home solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'home solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte.d.ts
index d8849ef..964224f 100644
--- a/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HomeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'home solid'
  * @prop ...restProps
  */
 declare const HomeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte
index 2b34a0d..b7df6fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'horizontal lines outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'horizontal lines outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte.d.ts
index add55f7..3c8f5fc 100644
--- a/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HorizontalLinesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'horizontal lines outline'
  * @prop ...restProps
  */
 declare const HorizontalLinesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte
index 99db401..e83a9cf 100644
--- a/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'hotdog outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'hotdog outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte.d.ts
index 1cf8857..48da37e 100644
--- a/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HotdogOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'hotdog outline'
  * @prop ...restProps
  */
 declare const HotdogOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte
index 14bb015..4995a55 100644
--- a/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'hotdog solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'hotdog solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte.d.ts
index 48c8381..afc81d3 100644
--- a/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HotdogSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'hotdog solid'
  * @prop ...restProps
  */
 declare const HotdogSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte b/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte
index 226dcbd..efff974 100644
--- a/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'hourglass outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'hourglass outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte.d.ts
index 99be279..4f21595 100644
--- a/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HourglassOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'hourglass outline'
  * @prop ...restProps
  */
 declare const HourglassOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte
index f54eb9e..64bfea8 100644
--- a/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'hourglass solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'hourglass solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte.d.ts
index 1f7b0d3..81d0a95 100644
--- a/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HourglassSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'hourglass solid'
  * @prop ...restProps
  */
 declare const HourglassSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte b/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte
index 31da945..5464beb 100644
--- a/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'html solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'html solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte.d.ts
index a14fb8d..2645151 100644
--- a/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/HtmlSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'html solid'
  * @prop ...restProps
  */
 declare const HtmlSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte
index f6a2dc9..dfbb9e7 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'icecream alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'icecream alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte.d.ts
index 4ee2836..2265ba1 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'icecream alt outline'
  * @prop ...restProps
  */
 declare const IcecreamAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte
index ef60c87..4285db2 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'icecream alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -58,6 +57,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'icecream alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte.d.ts
index 161efea..ea07ce2 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'icecream alt solid'
  * @prop ...restProps
  */
 declare const IcecreamAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte b/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte
index db60f11..59b4662 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'icecream outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'icecream outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte.d.ts
index ab5dd0d..57a7b5a 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'icecream outline'
  * @prop ...restProps
  */
 declare const IcecreamOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte b/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte
index 72c15ca..7b0aa41 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'icecream solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'icecream solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte.d.ts
index d02935f..029867a 100644
--- a/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IcecreamSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'icecream solid'
  * @prop ...restProps
  */
 declare const IcecreamSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte
index 79285d4..d6e0b1e 100644
--- a/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'image outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'image outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte.d.ts
index 4a443ef..1c5197d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ImageOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'image outline'
  * @prop ...restProps
  */
 declare const ImageOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte
index 9cd57c2..14f0e35 100644
--- a/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'image solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'image solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte.d.ts
index a432cee..d285542 100644
--- a/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ImageSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'image solid'
  * @prop ...restProps
  */
 declare const ImageSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte
index 14d3237..9ddf237 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'inbox full outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'inbox full outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte.d.ts
index 0d0046e..aee7d25 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InboxFullOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'inbox full outline'
  * @prop ...restProps
  */
 declare const InboxFullOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte b/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte
index 2a3c70a..e87c13c 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'inbox full solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'inbox full solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte.d.ts
index f227e29..5431677 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InboxFullSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'inbox full solid'
  * @prop ...restProps
  */
 declare const InboxFullSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte
index 0e4b73e..aee1031 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'inbox outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'inbox outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte.d.ts
index dac68e8..8a746b8 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InboxOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'inbox outline'
  * @prop ...restProps
  */
 declare const InboxOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte b/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte
index eb25e38..a84359b 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'inbox solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'inbox solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte.d.ts
index fc54525..2b02aa9 100644
--- a/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InboxSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'inbox solid'
  * @prop ...restProps
  */
 declare const InboxSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte b/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte
index 7e3d380..ffaa1ba 100644
--- a/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'incoming call outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'incoming call outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte.d.ts
index c737563..a21e8d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IncomingCallOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'incoming call outline'
  * @prop ...restProps
  */
 declare const IncomingCallOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte b/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte
index 038b31e..3ea8863 100644
--- a/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'incoming call solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'incoming call solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte.d.ts
index 83e88f9..d217783 100644
--- a/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IncomingCallSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'incoming call solid'
  * @prop ...restProps
  */
 declare const IncomingCallSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte b/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte
index cb29937..c044c8a 100644
--- a/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'indent outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'indent outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte.d.ts
index a655b21..d0561e8 100644
--- a/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IndentOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'indent outline'
  * @prop ...restProps
  */
 declare const IndentOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte b/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte
index 68a98ed..bbbdfa0 100644
--- a/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'indent solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'indent solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte.d.ts
index e36d4d5..d61b037 100644
--- a/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/IndentSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'indent solid'
  * @prop ...restProps
  */
 declare const IndentSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte
index 9c8aae3..64acae4 100644
--- a/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'info circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'info circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte.d.ts
index 2524312..34a32fc 100644
--- a/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InfoCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'info circle outline'
  * @prop ...restProps
  */
 declare const InfoCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte
index 239bbaa..4c515a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'info circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'info circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte.d.ts
index 0c836f6..b16a43e 100644
--- a/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InfoCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'info circle solid'
  * @prop ...restProps
  */
 declare const InfoCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte
index 287f001..c12f46b 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'insert row after outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'insert row after outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte.d.ts
index 2858d29..44b1388 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InsertRowAfterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'insert row after outline'
  * @prop ...restProps
  */
 declare const InsertRowAfterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte
index 7c2537d..0137156 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'insert row before outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'insert row before outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte.d.ts
index 55c6b9f..4b616ad 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InsertRowBeforeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'insert row before outline'
  * @prop ...restProps
  */
 declare const InsertRowBeforeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte
index a647fa4..c182fcb 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'insert table alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'insert table alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte.d.ts
index c44274c..9e7af4a 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InsertTableAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'insert table alt outline'
  * @prop ...restProps
  */
 declare const InsertTableAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte b/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte
index 3be6995..851069f 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'insert table outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'insert table outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte.d.ts
index c381466..efec881 100644
--- a/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InsertTableOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'insert table outline'
  * @prop ...restProps
  */
 declare const InsertTableOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte b/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte
index 1467422..b74cf9a 100644
--- a/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'instagram solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'instagram solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte.d.ts
index 967a099..16a67a1 100644
--- a/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/InstagramSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'instagram solid'
  * @prop ...restProps
  */
 declare const InstagramSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte
index 2a41183..5897d57 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'jar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'jar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte.d.ts
index 585fe9f..d0a9e4d 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/JarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'jar outline'
  * @prop ...restProps
  */
 declare const JarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte
index d273aad..14bf94a 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'jar solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'jar solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte.d.ts
index dc94c88..70db3c5 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/JarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'jar solid'
  * @prop ...restProps
  */
 declare const JarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte b/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte
index a98ecfc..a1fd349 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'jar wheat outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'jar wheat outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte.d.ts
index cb0f3ab..7fdc487 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/JarWheatOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'jar wheat outline'
  * @prop ...restProps
  */
 declare const JarWheatOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte b/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte
index 3e6ee0d..bce0c55 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'jar wheat solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'jar wheat solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte.d.ts
index 2fe5fe8..e665a03 100644
--- a/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/JarWheatSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'jar wheat solid'
  * @prop ...restProps
  */
 declare const JarWheatSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte
index e3fbf10..777e234 100644
--- a/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'keyboard outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -72,9 +71,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'keyboard outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte.d.ts
index 84fc176..6fc6536 100644
--- a/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/KeyboardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'keyboard outline'
  * @prop ...restProps
  */
 declare const KeyboardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte
index 4024360..6abc2b0 100644
--- a/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'keyboard solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'keyboard solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte.d.ts
index 9d2d8f2..2d02c0c 100644
--- a/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/KeyboardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'keyboard solid'
  * @prop ...restProps
  */
 declare const KeyboardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte
index ce6a712..1387141 100644
--- a/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'label outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'label outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte.d.ts
index 0e6d7b8..0c7270d 100644
--- a/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LabelOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'label outline'
  * @prop ...restProps
  */
 declare const LabelOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte
index 7b9e256..6af69d4 100644
--- a/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'label solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'label solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte.d.ts
index bf9074c..4c95903 100644
--- a/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LabelSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'label solid'
  * @prop ...restProps
  */
 declare const LabelSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte
index 8fb1fa4..d7b5ccc 100644
--- a/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'landmark outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'landmark outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte.d.ts
index 2bac298..d521c6f 100644
--- a/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LandmarkOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'landmark outline'
  * @prop ...restProps
  */
 declare const LandmarkOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte
index 0fb871f..85d8c09 100644
--- a/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'landmark solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'landmark solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte.d.ts
index 3666ee3..82bcc8e 100644
--- a/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LandmarkSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'landmark solid'
  * @prop ...restProps
  */
 declare const LandmarkSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte
index 480dd81..38fa3e5 100644
--- a/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'language outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'language outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte.d.ts
index 3f6104a..55521c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LanguageOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'language outline'
  * @prop ...restProps
  */
 declare const LanguageOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte
index 14b206f..c1940b3 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'laptop code outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'laptop code outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte.d.ts
index e02fc8c..02ac5ce 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopCodeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'laptop code outline'
  * @prop ...restProps
  */
 declare const LaptopCodeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte
index 2d6600e..850cc00 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'laptop code solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'laptop code solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte.d.ts
index adcea3d..b8ab383 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopCodeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'laptop code solid'
  * @prop ...restProps
  */
 declare const LaptopCodeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte
index 2212334..e7e8a0e 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'laptop file outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'laptop file outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte.d.ts
index b13313a..4ed959b 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopFileOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'laptop file outline'
  * @prop ...restProps
  */
 declare const LaptopFileOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte
index 942e74e..c338ba2 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'laptop file solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,6 +62,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'laptop file solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte.d.ts
index b0064eb..b24fdb7 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LaptopFileSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'laptop file solid'
  * @prop ...restProps
  */
 declare const LaptopFileSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte
index 624e4c5..cfb0cbf 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'laravel solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'laravel solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte.d.ts
index 891242d..80b70eb 100644
--- a/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LaravelSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'laravel solid'
  * @prop ...restProps
  */
 declare const LaravelSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte
index 3ce6d0d..563a7ee 100644
--- a/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'layers outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'layers outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte.d.ts
index f265be7..857cfd0 100644
--- a/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LayersOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'layers outline'
  * @prop ...restProps
  */
 declare const LayersOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte
index b74880d..5027ee9 100644
--- a/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'layers solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'layers solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte.d.ts
index 4c945c2..0b5c0ad 100644
--- a/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LayersSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'layers solid'
  * @prop ...restProps
  */
 declare const LayersSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte
index 3a7d5c8..05745be 100644
--- a/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lemon outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lemon outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte.d.ts
index dc73fa7..e5dc1a0 100644
--- a/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LemonOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lemon outline'
  * @prop ...restProps
  */
 declare const LemonOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte
index 2a634bd..597a1d7 100644
--- a/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lemon solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lemon solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte.d.ts
index bcd9fb9..f154c55 100644
--- a/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LemonSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lemon solid'
  * @prop ...restProps
  */
 declare const LemonSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte
index f57121b..2d33f2a 100644
--- a/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'letter bold outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'letter bold outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte.d.ts
index 9c35558..c23cc8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LetterBoldOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'letter bold outline'
  * @prop ...restProps
  */
 declare const LetterBoldOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte
index 6c1f187..5fae49b 100644
--- a/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'letter italic outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'letter italic outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte.d.ts
index c95ec20..91214e7 100644
--- a/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LetterItalicOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'letter italic outline'
  * @prop ...restProps
  */
 declare const LetterItalicOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte
index 3ddb0da..d24f2f6 100644
--- a/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'letter underline outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'letter underline outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte.d.ts
index 8b2db92..6961014 100644
--- a/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LetterUnderlineOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'letter underline outline'
  * @prop ...restProps
  */
 declare const LetterUnderlineOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte
index 2e72a53..0474846 100644
--- a/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'life saver outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'life saver outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte.d.ts
index f5ad6a8..008d775 100644
--- a/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LifeSaverOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'life saver outline'
  * @prop ...restProps
  */
 declare const LifeSaverOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte
index 35d8cc9..3b800e7 100644
--- a/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'life saver solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'life saver solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte.d.ts
index 9749959..a5bb611 100644
--- a/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LifeSaverSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'life saver solid'
  * @prop ...restProps
  */
 declare const LifeSaverSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte
index c4133b2..f69b5b1 100644
--- a/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lightbulb outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lightbulb outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte.d.ts
index ae143ef..7f6199e 100644
--- a/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LightbulbOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lightbulb outline'
  * @prop ...restProps
  */
 declare const LightbulbOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte
index cd172d0..0db8fa8 100644
--- a/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lightbulb solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lightbulb solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte.d.ts
index a99d428..2dc41c8 100644
--- a/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LightbulbSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lightbulb solid'
  * @prop ...restProps
  */
 declare const LightbulbSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte
index ae78d77..42f04cd 100644
--- a/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'link break outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'link break outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte.d.ts
index f0d9eb5..90a9aec 100644
--- a/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LinkBreakOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'link break outline'
  * @prop ...restProps
  */
 declare const LinkBreakOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte
index c4d4f60..cf428e3 100644
--- a/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'link outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'link outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte.d.ts
index f90160b..5dda30d 100644
--- a/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LinkOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'link outline'
  * @prop ...restProps
  */
 declare const LinkOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte
index c37b8dd..9b9cf21 100644
--- a/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'linkedin solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'linkedin solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte.d.ts
index 05e2700..b552d0d 100644
--- a/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LinkedinSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'linkedin solid'
  * @prop ...restProps
  */
 declare const LinkedinSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte
index 9e84c01..617464a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'list music outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'list music outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte.d.ts
index a401523..9f28490 100644
--- a/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ListMusicOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'list music outline'
  * @prop ...restProps
  */
 declare const ListMusicOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte
index 4214750..e302c00 100644
--- a/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'list music solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'list music solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte.d.ts
index fffb0e0..c80c3d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ListMusicSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'list music solid'
  * @prop ...restProps
  */
 declare const ListMusicSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte
index ae68c9e..4c77ffa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'list outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'list outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte.d.ts
index e4e1d4e..32a4b77 100644
--- a/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ListOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'list outline'
  * @prop ...restProps
  */
 declare const ListOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte
index e51d711..5b0915a 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lock open outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lock open outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte.d.ts
index ac28f18..5f39ea6 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LockOpenOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lock open outline'
  * @prop ...restProps
  */
 declare const LockOpenOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte
index fe34da8..2415e29 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lock open solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lock open solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte.d.ts
index c937090..c35d1c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LockOpenSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lock open solid'
  * @prop ...restProps
  */
 declare const LockOpenSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte
index f8c97ae..84b9c01 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lock outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lock outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte.d.ts
index f4aab00..3f9c0a6 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LockOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lock outline'
  * @prop ...restProps
  */
 declare const LockOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte
index dc7a97d..a2f6320 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lock solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lock solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte.d.ts
index a16d33b..3ada418 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LockSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lock solid'
  * @prop ...restProps
  */
 declare const LockSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte
index 4a0fb31..dcaf90b 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lock time outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lock time outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte.d.ts
index 14848cf..91c54d4 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LockTimeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lock time outline'
  * @prop ...restProps
  */
 declare const LockTimeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte
index ac52921..ac2c3dc 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'lock time solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'lock time solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte.d.ts
index 13de4a7..993b9df 100644
--- a/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/LockTimeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'lock time solid'
  * @prop ...restProps
  */
 declare const LockTimeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte
index 4b71d76..b820922 100644
--- a/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mail box outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mail box outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte.d.ts
index a32c2f2..8cd0a38 100644
--- a/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MailBoxOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mail box outline'
  * @prop ...restProps
  */
 declare const MailBoxOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte
index 86f2dfd..aa1e7b6 100644
--- a/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mail box solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mail box solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte.d.ts
index 5242e26..9be09ab 100644
--- a/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MailBoxSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mail box solid'
  * @prop ...restProps
  */
 declare const MailBoxSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte
index ecc86c9..2fc8d47 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'map pin alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -68,9 +67,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'map pin alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte.d.ts
index ad22506..aecd5e3 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'map pin alt outline'
  * @prop ...restProps
  */
 declare const MapPinAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte
index 4fa5da1..f5628e2 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'map pin alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'map pin alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte.d.ts
index dcc4336..f23f753 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'map pin alt solid'
  * @prop ...restProps
  */
 declare const MapPinAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte
index 158af8e..2d648dc 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'map pin outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'map pin outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte.d.ts
index e1eab6c..c6c28d0 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'map pin outline'
  * @prop ...restProps
  */
 declare const MapPinOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte
index bb4fdfe..0e389d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'map pin solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'map pin solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte.d.ts
index 84d63c7..001869d 100644
--- a/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MapPinSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'map pin solid'
  * @prop ...restProps
  */
 declare const MapPinSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte
index d83813e..f09e270 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'martini glass citrus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'martini glass citrus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte.d.ts
index c5529ec..c301ded 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'martini glass citrus outline'
  * @prop ...restProps
  */
 declare const MartiniGlassCitrusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte
index bf15f35..b78958f 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'martini glass citrus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'martini glass citrus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte.d.ts
index 1dbc700..4573976 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassCitrusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'martini glass citrus solid'
  * @prop ...restProps
  */
 declare const MartiniGlassCitrusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte
index 0629a1c..c4bf3e0 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'martini glass empty outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'martini glass empty outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte.d.ts
index 6d8fbfd..bdf9392 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'martini glass empty outline'
  * @prop ...restProps
  */
 declare const MartiniGlassEmptyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte
index 59b8da4..7d564b5 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'martini glass empty solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'martini glass empty solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte.d.ts
index 854e42b..119b03d 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassEmptySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'martini glass empty solid'
  * @prop ...restProps
  */
 declare const MartiniGlassEmptySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte
index 3e99f35..6100806 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'martini glass outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'martini glass outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte.d.ts
index 63912df..d7215a7 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'martini glass outline'
  * @prop ...restProps
  */
 declare const MartiniGlassOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte
index 404a6cd..2c9abc4 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'martini glass solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'martini glass solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte.d.ts
index e616e15..7e2aa87 100644
--- a/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MartiniGlassSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'martini glass solid'
  * @prop ...restProps
  */
 declare const MartiniGlassSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte
index 8a79b81..5f1246c 100644
--- a/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mastercard solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mastercard solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte.d.ts
index 74d45ad..d6d2a97 100644
--- a/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MastercardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mastercard solid'
  * @prop ...restProps
  */
 declare const MastercardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte
index 221f994..74028cb 100644
--- a/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'merge cells outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'merge cells outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte.d.ts
index abecb44..7942364 100644
--- a/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MergeCellsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'merge cells outline'
  * @prop ...restProps
  */
 declare const MergeCellsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte
index d38c1d8..0e06780 100644
--- a/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'merge or split outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'merge or split outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte.d.ts
index 5c2844e..b94776d 100644
--- a/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MergeOrSplitOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'merge or split outline'
  * @prop ...restProps
  */
 declare const MergeOrSplitOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte
index 8347e3c..cad72be 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'message caption outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'message caption outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte.d.ts
index 887ed38..916ef78 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MessageCaptionOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'message caption outline'
  * @prop ...restProps
  */
 declare const MessageCaptionOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte
index b7864d3..ca0ac63 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'message caption solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'message caption solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte.d.ts
index 8fb5eaa..0e771f7 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MessageCaptionSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'message caption solid'
  * @prop ...restProps
  */
 declare const MessageCaptionSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte
index 6694642..639f948 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'message dots outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'message dots outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte.d.ts
index e9733b8..9ca0044 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MessageDotsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'message dots outline'
  * @prop ...restProps
  */
 declare const MessageDotsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte
index eca2e06..8884ee3 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'message dots solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'message dots solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte.d.ts
index 9b5611c..59cf55e 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MessageDotsSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'message dots solid'
  * @prop ...restProps
  */
 declare const MessageDotsSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte
index 1c88258..b84e978 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'messages outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'messages outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte.d.ts
index 8e2f67f..9b420d5 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MessagesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'messages outline'
  * @prop ...restProps
  */
 declare const MessagesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte
index 9206e98..3b6423b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'messages solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'messages solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte.d.ts
index 5e9e40c..640667e 100644
--- a/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MessagesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'messages solid'
  * @prop ...restProps
  */
 declare const MessagesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte
index da03504..3fdd318 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'microphone outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'microphone outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte.d.ts
index bffd4de..8271e65 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'microphone outline'
  * @prop ...restProps
  */
 declare const MicrophoneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte
index d0e5fa6..eee783b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,13 +19,12 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'microphone slash outline',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -32,11 +32,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -58,6 +57,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'microphone slash outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte.d.ts
index 3783ceb..f559817 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashOutline.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'microphone slash outline'
  * @prop ...restProps
  */
 declare const MicrophoneSlashOutline: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte
index b0927fa..695b3b7 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'microphone slash solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'microphone slash solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte.d.ts
index 8ffba7e..6d6e92a 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneSlashSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'microphone slash solid'
  * @prop ...restProps
  */
 declare const MicrophoneSlashSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte
index 42bf649..f00e35a 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'microphone solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'microphone solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte.d.ts
index 5d849d9..97f2fb2 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MicrophoneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'microphone solid'
  * @prop ...restProps
  */
 declare const MicrophoneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte
index 56b9fa4..4382f09 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'microscope outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'microscope outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte.d.ts
index 0fbbe42..6a66a56 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MicroscopeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'microscope outline'
  * @prop ...restProps
  */
 declare const MicroscopeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte
index ffe276e..9ea6474 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'microscope solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'microscope solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte.d.ts
index 9b51990..df757bd 100644
--- a/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MicroscopeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'microscope solid'
  * @prop ...restProps
  */
 declare const MicroscopeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte
index 66caa9a..70cc7a7 100644
--- a/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'minimize outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'minimize outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte.d.ts
index 1fa2508..d63a26b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MinimizeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'minimize outline'
  * @prop ...restProps
  */
 declare const MinimizeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte
index 8ac3aed..591ad55 100644
--- a/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'minus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'minus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte.d.ts
index ee2a6a8..3ddc002 100644
--- a/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MinusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'minus outline'
  * @prop ...restProps
  */
 declare const MinusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte
index 2ad2c5a..e8a0cbe 100644
--- a/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'missed call outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'missed call outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte.d.ts
index e1ad90d..43fe4c2 100644
--- a/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MissedCallOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'missed call outline'
  * @prop ...restProps
  */
 declare const MissedCallOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte
index 3e142f3..393f2fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'missed call solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'missed call solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte.d.ts
index 0892e52..428710b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MissedCallSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'missed call solid'
  * @prop ...restProps
  */
 declare const MissedCallSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte
index e267402..ff742c4 100644
--- a/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mobile phone outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mobile phone outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte.d.ts
index 24dc168..46219ba 100644
--- a/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MobilePhoneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mobile phone outline'
  * @prop ...restProps
  */
 declare const MobilePhoneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte
index e0753fc..64bf1dc 100644
--- a/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mobile phone solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mobile phone solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte.d.ts
index 895627f..055570e 100644
--- a/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MobilePhoneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mobile phone solid'
  * @prop ...restProps
  */
 declare const MobilePhoneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte
index 3972ec5..15c246b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mongo db solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mongo db solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte.d.ts
index 5e5884f..6f0eb0e 100644
--- a/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MongoDbSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mongo db solid'
  * @prop ...restProps
  */
 declare const MongoDbSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte
index 4f49851..e665c1f 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'moon outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'moon outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte.d.ts
index 49ac358..fc80a4c 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MoonOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'moon outline'
  * @prop ...restProps
  */
 declare const MoonOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte
index 753f6b5..e12cb91 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'moon plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'moon plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte.d.ts
index 9273ed3..dbb8cef 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MoonPlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'moon plus outline'
  * @prop ...restProps
  */
 declare const MoonPlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte
index b55293d..1a0e7e9 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'moon plus solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'moon plus solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte.d.ts
index 1149190..edac688 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MoonPlusSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'moon plus solid'
  * @prop ...restProps
  */
 declare const MoonPlusSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte
index fbd9beb..6910e32 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'moon solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'moon solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte.d.ts
index 8ba6815..eed3796 100644
--- a/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MoonSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'moon solid'
  * @prop ...restProps
  */
 declare const MoonSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte
index b5dfe3f..53e9e32 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mug hot outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mug hot outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte.d.ts
index 7dc4ef7..6d2530b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MugHotOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mug hot outline'
  * @prop ...restProps
  */
 declare const MugHotOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte
index f3a3a61..bf5923b 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mug hot solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mug hot solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte.d.ts
index 16f33d2..7d4ffd1 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MugHotSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mug hot solid'
  * @prop ...restProps
  */
 declare const MugHotSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte
index d4f649f..57d911f 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mug saucer outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mug saucer outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte.d.ts
index 2cb7f0b..0de585c 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MugSaucerOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mug saucer outline'
  * @prop ...restProps
  */
 declare const MugSaucerOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte
index 27108db..44ea000 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'mug saucer solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'mug saucer solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte.d.ts
index 1f4db55..1766a21 100644
--- a/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MugSaucerSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'mug saucer solid'
  * @prop ...restProps
  */
 declare const MugSaucerSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte
index ac4f797..02248a5 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'music alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'music alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte.d.ts
index b865f1e..6397d46 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MusicAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'music alt outline'
  * @prop ...restProps
  */
 declare const MusicAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte
index 58e2786..3aacb58 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'music alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'music alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte.d.ts
index 7fab644..edf68c4 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MusicAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'music alt solid'
  * @prop ...restProps
  */
 declare const MusicAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte b/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte
index c876f23..265d2a7 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'music outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'music outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte.d.ts
index 73a230b..c1ac7aa 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MusicOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'music outline'
  * @prop ...restProps
  */
 declare const MusicOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte b/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte
index 4f26de6..dd6d9ba 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'music solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'music solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte.d.ts
index d868e2f..28b0fa8 100644
--- a/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/MusicSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'music solid'
  * @prop ...restProps
  */
 declare const MusicSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte b/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte
index 92e272a..3ac6551 100644
--- a/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'newspaper outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'newspaper outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte.d.ts
index 2377c9a..628fa1b 100644
--- a/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/NewspaperOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'newspaper outline'
  * @prop ...restProps
  */
 declare const NewspaperOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte b/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte
index cd25d16..5e85eb8 100644
--- a/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'newspaper solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'newspaper solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte.d.ts
index 2065f7e..73c39b2 100644
--- a/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/NewspaperSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'newspaper solid'
  * @prop ...restProps
  */
 declare const NewspaperSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte b/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte
index 89d7258..977235b 100644
--- a/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,27 +17,25 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'npm solid',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'npm solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte.d.ts
index 88fface..7859c22 100644
--- a/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/NpmSolid.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'npm solid'
  * @prop ...restProps
  */
 declare const NpmSolid: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte
index 634c7ae..3253c83 100644
--- a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'objects column outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'objects column outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte.d.ts
index d023643..5e38ffa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'objects column outline'
  * @prop ...restProps
  */
 declare const ObjectsColumnOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte
index 5cbfcd8..972c1d5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'objects column solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'objects column solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte.d.ts
index dd3426d..d5da890 100644
--- a/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ObjectsColumnSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'objects column solid'
  * @prop ...restProps
  */
 declare const ObjectsColumnSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte b/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte
index 73b4c2d..15aadae 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'open door outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'open door outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte.d.ts
index 2afdd79..bd1df04 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OpenDoorOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'open door outline'
  * @prop ...restProps
  */
 declare const OpenDoorOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte b/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte
index d7e0662..81f735b 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'open door solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'open door solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte.d.ts
index a084168..35bba1f 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OpenDoorSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'open door solid'
  * @prop ...restProps
  */
 declare const OpenDoorSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte
index 6c1576a..1699d62 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'open sidebar alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'open sidebar alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte.d.ts
index d38e747..b9bdb9d 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'open sidebar alt outline'
  * @prop ...restProps
  */
 declare const OpenSidebarAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte
index 93fc6c3..fae7f50 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'open sidebar alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'open sidebar alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte.d.ts
index 0c63b78..9b33a32 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'open sidebar alt solid'
  * @prop ...restProps
  */
 declare const OpenSidebarAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte
index 9098c1c..963bc94 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'open sidebar outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'open sidebar outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte.d.ts
index ca80a90..91c5058 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'open sidebar outline'
  * @prop ...restProps
  */
 declare const OpenSidebarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte
index 81e6acd..5cd6a0e 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'open sidebar solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'open sidebar solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte.d.ts
index bd6c909..bec5850 100644
--- a/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OpenSidebarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'open sidebar solid'
  * @prop ...restProps
  */
 declare const OpenSidebarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte b/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte
index 1028f9f..bee5048 100644
--- a/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'ordered list outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'ordered list outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte.d.ts
index c64ad2a..f7cd5b2 100644
--- a/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OrderedListOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'ordered list outline'
  * @prop ...restProps
  */
 declare const OrderedListOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte b/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte
index dd8bdc5..0681106 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'outdent outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'outdent outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte.d.ts
index a0f11ba..da29f3e 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OutdentOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'outdent outline'
  * @prop ...restProps
  */
 declare const OutdentOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte b/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte
index 47ffbd8..67f854e 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'outdent solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'outdent solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte.d.ts
index 5379c08..c7f7401 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OutdentSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'outdent solid'
  * @prop ...restProps
  */
 declare const OutdentSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte b/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte
index 4e0ed1f..58e705f 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'outgoing call outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'outgoing call outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte.d.ts
index b4b5408..ecad272 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OutgoingCallOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'outgoing call outline'
  * @prop ...restProps
  */
 declare const OutgoingCallOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte b/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte
index e405f54..2070dd0 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'outgoing call solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'outgoing call solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte.d.ts
index 2fb7a29..fce6709 100644
--- a/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/OutgoingCallSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'outgoing call solid'
  * @prop ...restProps
  */
 declare const OutgoingCallSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte
index 5cbb0fd..d02cee6 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'palette outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'palette outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte.d.ts
index 4d59c00..aa8e805 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PaletteOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'palette outline'
  * @prop ...restProps
  */
 declare const PaletteOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte
index 3a26db5..b8a2c7e 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'palette solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'palette solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte.d.ts
index 04b4c2e..6fc29b7 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PaletteSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'palette solid'
  * @prop ...restProps
  */
 declare const PaletteSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte
index ed195e1..72664b9 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'paper clip outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'paper clip outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte.d.ts
index 5fda0e7..619438f 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PaperClipOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'paper clip outline'
  * @prop ...restProps
  */
 declare const PaperClipOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte
index b11b88a..e957057 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'paper plane outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'paper plane outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte.d.ts
index e77f18a..8679611 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PaperPlaneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'paper plane outline'
  * @prop ...restProps
  */
 declare const PaperPlaneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte
index 9710afd..711f834 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'paper plane solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'paper plane solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte.d.ts
index 15fcf5a..f2ae397 100644
--- a/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PaperPlaneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'paper plane solid'
  * @prop ...restProps
  */
 declare const PaperPlaneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte
index 374ad2e..5b9d1d5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'paragraph outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'paragraph outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte.d.ts
index 190f1af..7ab2f19 100644
--- a/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ParagraphOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'paragraph outline'
  * @prop ...restProps
  */
 declare const ParagraphOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte
index 9244cc5..645c8c4 100644
--- a/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'paragraph solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'paragraph solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte.d.ts
index a09aaa8..5527fc5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ParagraphSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'paragraph solid'
  * @prop ...restProps
  */
 declare const ParagraphSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte
index 4590c44..e4ef9dc 100644
--- a/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pause outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pause outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte.d.ts
index 08e4e43..d275371 100644
--- a/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PauseOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pause outline'
  * @prop ...restProps
  */
 declare const PauseOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte
index fb73bc3..80cfa6b 100644
--- a/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pause solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pause solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte.d.ts
index 6821993..d2d594e 100644
--- a/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PauseSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pause solid'
  * @prop ...restProps
  */
 declare const PauseSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte
index 1efc0f7..31749d4 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pen nib outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pen nib outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte.d.ts
index c1e68fc..633768a 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PenNibOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pen nib outline'
  * @prop ...restProps
  */
 declare const PenNibOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte
index 2e30489..f5a19f1 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pen nib solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pen nib solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte.d.ts
index 362665d..4f2b19b 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PenNibSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pen nib solid'
  * @prop ...restProps
  */
 declare const PenNibSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte
index d8a01e1..6fccdc1 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pen outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pen outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte.d.ts
index de51950..9e27fa8 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PenOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pen outline'
  * @prop ...restProps
  */
 declare const PenOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte
index c669085..dc1845e 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pen solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pen solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte.d.ts
index 02928aa..efb6d81 100644
--- a/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PenSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pen solid'
  * @prop ...restProps
  */
 declare const PenSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte
index 2e2eb12..bfce079 100644
--- a/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pepper hot outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pepper hot outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte.d.ts
index 13f22fa..30c48f0 100644
--- a/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PepperHotOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pepper hot outline'
  * @prop ...restProps
  */
 declare const PepperHotOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte
index 1e53a8d..129323a 100644
--- a/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pepper hot solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pepper hot solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte.d.ts
index 7f23f99..9d7ffee 100644
--- a/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PepperHotSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pepper hot solid'
  * @prop ...restProps
  */
 declare const PepperHotSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte
index 5d37f37..26d95d8 100644
--- a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'person chalkboard outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'person chalkboard outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte.d.ts
index c87c67d..00f2313 100644
--- a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'person chalkboard outline'
  * @prop ...restProps
  */
 declare const PersonChalkboardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte
index b5e6f6e..b40abd5 100644
--- a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'person chalkboard solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,6 +60,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'person chalkboard solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte.d.ts
index 68112cf..df65c38 100644
--- a/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PersonChalkboardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'person chalkboard solid'
  * @prop ...restProps
  */
 declare const PersonChalkboardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte
index 81eb3e1..07e0c77 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'phone hangup outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'phone hangup outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte.d.ts
index ad4189c..d3ddd72 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneHangupOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'phone hangup outline'
  * @prop ...restProps
  */
 declare const PhoneHangupOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte
index eabdb2a..022a73c 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'phone hangup solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'phone hangup solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte.d.ts
index b625523..5adbbc2 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneHangupSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'phone hangup solid'
  * @prop ...restProps
  */
 declare const PhoneHangupSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte
index 6d768d7..94222cd 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'phone outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'phone outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte.d.ts
index 44dfd88..071ad57 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'phone outline'
  * @prop ...restProps
  */
 declare const PhoneOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte
index 04b62f9..181c4d1 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'phone solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'phone solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte.d.ts
index c2821d9..f255806 100644
--- a/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PhoneSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'phone solid'
  * @prop ...restProps
  */
 declare const PhoneSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte
index f605efc..6e7257d 100644
--- a/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pizza slice outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,9 +62,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pizza slice outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte.d.ts
index 689b489..6ef2970 100644
--- a/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PizzaSliceOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pizza slice outline'
  * @prop ...restProps
  */
 declare const PizzaSliceOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte
index 18729ac..4c62408 100644
--- a/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'pizza slice solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'pizza slice solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte.d.ts
index e6831eb..420e93e 100644
--- a/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PizzaSliceSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'pizza slice solid'
  * @prop ...restProps
  */
 declare const PizzaSliceSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte
index 54453cc..239a104 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'plate wheat outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'plate wheat outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte.d.ts
index 91887e7..0e353ee 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PlateWheatOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'plate wheat outline'
  * @prop ...restProps
  */
 declare const PlateWheatOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte
index 3b84674..0913112 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'plate wheat solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'plate wheat solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte.d.ts
index fe4b323..00ce4c0 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PlateWheatSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'plate wheat solid'
  * @prop ...restProps
  */
 declare const PlateWheatSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte
index 5258100..95bccac 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'play outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'play outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte.d.ts
index 5bf7287..e24f90a 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PlayOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'play outline'
  * @prop ...restProps
  */
 declare const PlayOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte b/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte
index af5f872..00430c1 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'play solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'play solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte.d.ts
index e6b6bff..3bc987a 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PlaySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'play solid'
  * @prop ...restProps
  */
 declare const PlaySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte
index 9bb0243..fe1d6b7 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'plus outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'plus outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte.d.ts
index cea25f1..824d5d7 100644
--- a/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PlusOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'plus outline'
  * @prop ...restProps
  */
 declare const PlusOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte b/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte
index d8f7550..d4b5560 100644
--- a/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'printer outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'printer outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte.d.ts
index 5131cce..95f36a5 100644
--- a/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PrinterOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'printer outline'
  * @prop ...restProps
  */
 declare const PrinterOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte b/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte
index da09912..c1cae55 100644
--- a/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'printer solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'printer solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte.d.ts
index 5078bd7..b9e42c9 100644
--- a/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/PrinterSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'printer solid'
  * @prop ...restProps
  */
 declare const PrinterSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte
index 43aad63..15abde2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'profile card outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'profile card outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte.d.ts
index 820e68d..0a7c93a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ProfileCardOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'profile card outline'
  * @prop ...restProps
  */
 declare const ProfileCardOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte
index 4b660d3..aa7164a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'profile card solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'profile card solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte.d.ts
index 27722f3..092620a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ProfileCardSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'profile card solid'
  * @prop ...restProps
  */
 declare const ProfileCardSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte
index ecec2a5..d0c647a 100644
--- a/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'qr code outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -66,9 +65,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'qr code outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte.d.ts
index dbb18dc..e5538b3 100644
--- a/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/QrCodeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'qr code outline'
  * @prop ...restProps
  */
 declare const QrCodeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte
index f62dbac..b87a77c 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'question circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'question circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte.d.ts
index 528d5a8..e0f3723 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/QuestionCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'question circle outline'
  * @prop ...restProps
  */
 declare const QuestionCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte
index bff0245..40f62e2 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'question circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'question circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte.d.ts
index 466a080..5fc4dcf 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/QuestionCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'question circle solid'
  * @prop ...restProps
  */
 declare const QuestionCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte b/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte
index 5bee063..5d9431b 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'quote outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'quote outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte.d.ts
index 316e818..9aa6c69 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/QuoteOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'quote outline'
  * @prop ...restProps
  */
 declare const QuoteOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte b/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte
index 57adfe9..a2f97bc 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'quote solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'quote solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte.d.ts
index b63a6a6..45980a9 100644
--- a/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/QuoteSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'quote solid'
  * @prop ...restProps
  */
 declare const QuoteSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte
index 1604a18..4a649ee 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'react solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'react solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte.d.ts
index b08bb10..f92058c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReactSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'react solid'
  * @prop ...restProps
  */
 declare const ReactSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte
index d55dad6..898f45d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'receipt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'receipt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte.d.ts
index af70fc1..8585a79 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReceiptOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'receipt outline'
  * @prop ...restProps
  */
 declare const ReceiptOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte
index 75940d6..fb1000c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'receipt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'receipt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte.d.ts
index fe46a56..f60381e 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReceiptSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'receipt solid'
  * @prop ...restProps
  */
 declare const ReceiptSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte b/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte
index b92ae83..3fc551d 100644
--- a/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'rectangle list outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'rectangle list outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte.d.ts
index 6fcc72d..bd0cc35 100644
--- a/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RectangleListOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'rectangle list outline'
  * @prop ...restProps
  */
 declare const RectangleListOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte b/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte
index 7a3ef1d..d28314b 100644
--- a/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'rectangle list solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'rectangle list solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte.d.ts
index 1fa9288..545a996 100644
--- a/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RectangleListSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'rectangle list solid'
  * @prop ...restProps
  */
 declare const RectangleListSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte b/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte
index 6541579..02bf639 100644
--- a/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'reddit solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'reddit solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte.d.ts
index a5369e1..50a2045 100644
--- a/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RedditSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'reddit solid'
  * @prop ...restProps
  */
 declare const RedditSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte b/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte
index 5637050..1cdbc7e 100644
--- a/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'redo outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'redo outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte.d.ts
index c9da139..cd8a15f 100644
--- a/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RedoOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'redo outline'
  * @prop ...restProps
  */
 declare const RedoOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte b/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte
index 865aef0..65179d4 100644
--- a/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'refresh outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'refresh outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte.d.ts
index 040e994..c997bb8 100644
--- a/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RefreshOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'refresh outline'
  * @prop ...restProps
  */
 declare const RefreshOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte
index 76efd10..82819ed 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'reply all outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'reply all outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte.d.ts
index 01b7300..47171da 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReplyAllOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'reply all outline'
  * @prop ...restProps
  */
 declare const ReplyAllOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte
index 9036af8..cc4b7be 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'reply all solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'reply all solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte.d.ts
index 7e4b14e..50bb006 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReplyAllSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'reply all solid'
  * @prop ...restProps
  */
 declare const ReplyAllSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte
index f7980ab..2324258 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'reply outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'reply outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte.d.ts
index 0d88657..73276f8 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReplyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'reply outline'
  * @prop ...restProps
  */
 declare const ReplyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte b/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte
index e8d2263..86742ca 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'reply solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'reply solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte.d.ts
index 498e337..39e9bb3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ReplySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'reply solid'
  * @prop ...restProps
  */
 declare const ReplySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte b/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte
index c414dec..9c1860e 100644
--- a/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'restore window outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'restore window outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte.d.ts
index 246a642..9bec079 100644
--- a/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RestoreWindowOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'restore window outline'
  * @prop ...restProps
  */
 declare const RestoreWindowOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte b/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte
index c4dfec6..fb4917a 100644
--- a/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'rocket outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'rocket outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte.d.ts
index 957dc00..76c79be 100644
--- a/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RocketOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'rocket outline'
  * @prop ...restProps
  */
 declare const RocketOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte b/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte
index 8855530..091f152 100644
--- a/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'rocket solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'rocket solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte.d.ts
index 042f94f..5c4d73a 100644
--- a/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RocketSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'rocket solid'
  * @prop ...restProps
  */
 declare const RocketSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte b/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte
index 21be7ab..9edd59a 100644
--- a/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'ruler combined outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'ruler combined outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte.d.ts
index 545f4ee..8494773 100644
--- a/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RulerCombinedOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'ruler combined outline'
  * @prop ...restProps
  */
 declare const RulerCombinedOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte b/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte
index 2c48055..0497411 100644
--- a/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'ruler combined solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'ruler combined solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte.d.ts
index b4e353a..744fd02 100644
--- a/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/RulerCombinedSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'ruler combined solid'
  * @prop ...restProps
  */
 declare const RulerCombinedSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte
index 07b3a52..d3c366b 100644
--- a/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'sale percent outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'sale percent outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte.d.ts
index 102d4cf..9d9e135 100644
--- a/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SalePercentOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'sale percent outline'
  * @prop ...restProps
  */
 declare const SalePercentOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte
index 5c453f2..42431c7 100644
--- a/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'sale percent solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'sale percent solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte.d.ts
index 16bee46..2809bf6 100644
--- a/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SalePercentSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'sale percent solid'
  * @prop ...restProps
  */
 declare const SalePercentSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte
index 7d6dc42..aa3e117 100644
--- a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'scale balanced outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'scale balanced outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte.d.ts
index b4e9ee6..59e4229 100644
--- a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'scale balanced outline'
  * @prop ...restProps
  */
 declare const ScaleBalancedOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte
index 4fcaf16..c52a1fe 100644
--- a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'scale balanced solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'scale balanced solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte.d.ts
index 7f475dc..6142796 100644
--- a/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ScaleBalancedSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'scale balanced solid'
  * @prop ...restProps
  */
 declare const ScaleBalancedSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte
index 174681f..03af918 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte.d.ts
index 6b979a6..f6d8c0a 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school alt outline'
  * @prop ...restProps
  */
 declare const SchoolAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte
index 7397d49..51748fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -63,6 +62,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte.d.ts
index 2e638fb..9ffc757 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school alt solid'
  * @prop ...restProps
  */
 declare const SchoolAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte
index 25a5500..ab504a9 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school check alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school check alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte.d.ts
index 139acea..04c1733 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school check alt outline'
  * @prop ...restProps
  */
 declare const SchoolCheckAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte
index 2f7940b..59dedbb 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school check alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -65,6 +64,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school check alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte.d.ts
index 4e1191f..bc735f7 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school check alt solid'
  * @prop ...restProps
  */
 declare const SchoolCheckAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte
index 0f0a6a7..e3719b5 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school check outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school check outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte.d.ts
index 6867388..e6b8151 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school check outline'
  * @prop ...restProps
  */
 declare const SchoolCheckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte
index 46f8937..60e067d 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school check solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -67,6 +66,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school check solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte.d.ts
index de5f050..904def7 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolCheckSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school check solid'
  * @prop ...restProps
  */
 declare const SchoolCheckSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte
index 7d381be..c196401 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school exclamation alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school exclamation alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte.d.ts
index 8e5d5a2..632183b 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school exclamation alt outline'
  * @prop ...restProps
  */
 declare const SchoolExclamationAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte
index eadd1c4..118d1cc 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school exclamation alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -67,6 +66,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school exclamation alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte.d.ts
index e006f93..53ed549 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school exclamation alt solid'
  * @prop ...restProps
  */
 declare const SchoolExclamationAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte
index 961faf9..462aefb 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school exclamation outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school exclamation outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte.d.ts
index a48130f..4854208 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school exclamation outline'
  * @prop ...restProps
  */
 declare const SchoolExclamationOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte
index f5d3af6..20809bd 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school exclamation solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school exclamation solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte.d.ts
index 98cbef3..4904d22 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolExclamationSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school exclamation solid'
  * @prop ...restProps
  */
 declare const SchoolExclamationSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte
index 13f00c0..c320573 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school flag alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school flag alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte.d.ts
index 2f0479a..df66b99 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school flag alt outline'
  * @prop ...restProps
  */
 declare const SchoolFlagAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte
index bc1ef05..9ebc5c5 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school flag alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school flag alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte.d.ts
index 2927bba..fd979ba 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school flag alt solid'
  * @prop ...restProps
  */
 declare const SchoolFlagAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte
index c4a591d..f826af9 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school flag outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school flag outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte.d.ts
index 9a51363..de7c319 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school flag outline'
  * @prop ...restProps
  */
 declare const SchoolFlagOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte
index 21ec51a..3b6a408 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school flag solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school flag solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte.d.ts
index 2e2f391..da5fb76 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolFlagSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school flag solid'
  * @prop ...restProps
  */
 declare const SchoolFlagSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte
index a0e301c..0473135 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school lock alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school lock alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte.d.ts
index ffbfe0e..e089d55 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school lock alt outline'
  * @prop ...restProps
  */
 declare const SchoolLockAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte
index 69e166d..920feb0 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school lock alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -65,6 +64,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school lock alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte.d.ts
index fa76411..0f2c4fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school lock alt solid'
  * @prop ...restProps
  */
 declare const SchoolLockAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte
index c4d916b..12700ab 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school lock outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school lock outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte.d.ts
index 6c294ab..2c75ae3 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school lock outline'
  * @prop ...restProps
  */
 declare const SchoolLockOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte
index 7b3c3a2..378d7ae 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school lock solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -67,6 +66,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school lock solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte.d.ts
index 14b4bed..822fe51 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolLockSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school lock solid'
  * @prop ...restProps
  */
 declare const SchoolLockSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte
index f0aefdb..34da7f0 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte.d.ts
index 7477da2..173ad5f 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school outline'
  * @prop ...restProps
  */
 declare const SchoolOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte
index b460c6f..2860037 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte.d.ts
index dadaa5b..a7c366a 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school solid'
  * @prop ...restProps
  */
 declare const SchoolSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte
index de49ce9..3713319 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school xmark alt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school xmark alt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte.d.ts
index f224b82..c12bbbd 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school xmark alt outline'
  * @prop ...restProps
  */
 declare const SchoolXmarkAltOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte
index 28f6b31..69e9277 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school xmark alt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school xmark alt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte.d.ts
index a441c8b..d217bc7 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkAltSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school xmark alt solid'
  * @prop ...restProps
  */
 declare const SchoolXmarkAltSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte
index ced0841..209d19d 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school xmark outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school xmark outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte.d.ts
index c19e0e5..930ecb6 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school xmark outline'
  * @prop ...restProps
  */
 declare const SchoolXmarkOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte
index 3d8932d..3c8190f 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'school xmark solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -67,6 +66,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'school xmark solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte.d.ts
index 53880cf..509c6cd 100644
--- a/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SchoolXmarkSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'school xmark solid'
  * @prop ...restProps
  */
 declare const SchoolXmarkSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte
index 90027ec..f338660 100644
--- a/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'search outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'search outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte.d.ts
index 7bd4d67..211f8a8 100644
--- a/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SearchOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'search outline'
  * @prop ...restProps
  */
 declare const SearchOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte
index 9416fc1..f80b4c6 100644
--- a/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'search solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'search solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte.d.ts
index 2246644..b40be99 100644
--- a/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SearchSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'search solid'
  * @prop ...restProps
  */
 declare const SearchSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte
index 197c576..5f85532 100644
--- a/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'seedling outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'seedling outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte.d.ts
index 5488108..219effe 100644
--- a/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SeedlingOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'seedling outline'
  * @prop ...restProps
  */
 declare const SeedlingOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte
index 53daff4..0b1bc4c 100644
--- a/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'seedling solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'seedling solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte.d.ts
index 38ee2c6..110da68 100644
--- a/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SeedlingSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'seedling solid'
  * @prop ...restProps
  */
 declare const SeedlingSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte
index 94b5fa9..47c92ff 100644
--- a/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'server outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'server outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte.d.ts
index 482d59b..4c0ff10 100644
--- a/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ServerOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'server outline'
  * @prop ...restProps
  */
 declare const ServerOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte
index 7dd7790..9f376fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'server solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'server solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte.d.ts
index 8d82c80..0924628 100644
--- a/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ServerSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'server solid'
  * @prop ...restProps
  */
 declare const ServerSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte
index 87063c5..0c940ab 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shapes outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shapes outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte.d.ts
index c2feb62..b8e04aa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShapesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shapes outline'
  * @prop ...restProps
  */
 declare const ShapesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte
index 349f5f5..fdb8a94 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shapes solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shapes solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte.d.ts
index d40b7ec..035ca64 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShapesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shapes solid'
  * @prop ...restProps
  */
 declare const ShapesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte
index de0f7d9..ffbd0f5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'share all outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'share all outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte.d.ts
index cd285f5..958c802 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShareAllOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'share all outline'
  * @prop ...restProps
  */
 declare const ShareAllOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte
index fd694ca..f4fd026 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'share all solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'share all solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte.d.ts
index 3410775..7aa123f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShareAllSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'share all solid'
  * @prop ...restProps
  */
 declare const ShareAllSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte
index b34d91d..cd1de9e 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'share nodes outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'share nodes outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte.d.ts
index e7adb52..e952639 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShareNodesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'share nodes outline'
  * @prop ...restProps
  */
 declare const ShareNodesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte
index c189986..2683254 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'share nodes solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'share nodes solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte.d.ts
index db7c292..90ecd00 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShareNodesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'share nodes solid'
  * @prop ...restProps
  */
 declare const ShareNodesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte
index 33d131d..d40a98c 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shield check outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shield check outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte.d.ts
index bad015f..485ae99 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldCheckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shield check outline'
  * @prop ...restProps
  */
 declare const ShieldCheckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte
index 6941503..23fd0a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shield check solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shield check solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte.d.ts
index 9abb2dc..9a4d8b2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldCheckSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shield check solid'
  * @prop ...restProps
  */
 declare const ShieldCheckSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte
index 5c761de..a08d0e1 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shield outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shield outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte.d.ts
index 4bcc47d..f3a9335 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shield outline'
  * @prop ...restProps
  */
 declare const ShieldOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte
index f356ca3..a8ffc3d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shield solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shield solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte.d.ts
index bf55048..cd71ca9 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShieldSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shield solid'
  * @prop ...restProps
  */
 declare const ShieldSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte
index 2dc5362..34c4581 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shopping bag outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shopping bag outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte.d.ts
index 393f51c..f98de2f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShoppingBagOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shopping bag outline'
  * @prop ...restProps
  */
 declare const ShoppingBagOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte
index 39b15d4..104eebd 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shopping bag solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shopping bag solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte.d.ts
index 3f5e714..48abe8f 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShoppingBagSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shopping bag solid'
  * @prop ...restProps
  */
 declare const ShoppingBagSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte
index 3664d58..27ac502 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shrimp outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shrimp outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte.d.ts
index 5de2746..3ea5044 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShrimpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shrimp outline'
  * @prop ...restProps
  */
 declare const ShrimpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte
index be08064..8411390 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shrimp solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shrimp solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte.d.ts
index 3b85eff..90819b7 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShrimpSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shrimp solid'
  * @prop ...restProps
  */
 declare const ShrimpSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte
index d8063fb..681d375 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'shuffle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'shuffle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte.d.ts
index ea554c1..3c5dc8e 100644
--- a/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ShuffleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'shuffle outline'
  * @prop ...restProps
  */
 declare const ShuffleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte
index 4cf1946..5da59d7 100644
--- a/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'sort horizontal outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'sort horizontal outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte.d.ts
index a6833c2..d4fa278 100644
--- a/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SortHorizontalOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'sort horizontal outline'
  * @prop ...restProps
  */
 declare const SortHorizontalOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte
index a06d18b..9b93a53 100644
--- a/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'sort outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'sort outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte.d.ts
index e8725bc..0131767 100644
--- a/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SortOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'sort outline'
  * @prop ...restProps
  */
 declare const SortOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte
index 1752618..bcc5ca1 100644
--- a/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'split cells outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'split cells outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte.d.ts
index 8f33ccd..b025db9 100644
--- a/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SplitCellsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'split cells outline'
  * @prop ...restProps
  */
 declare const SplitCellsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte
index faa0e8c..3067402 100644
--- a/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'stackoverflow solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -58,6 +57,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'stackoverflow solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte.d.ts
index a1e30ef..e7fd7bc 100644
--- a/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StackoverflowSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'stackoverflow solid'
  * @prop ...restProps
  */
 declare const StackoverflowSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte b/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte
index 4102caf..9454793 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'star half outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'star half outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte.d.ts
index b5b3242..6fc71e5 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'star half outline'
  * @prop ...restProps
  */
 declare const StarHalfOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte
index a44031d..878f994 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'star half solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'star half solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte.d.ts
index fa44938..939cba6 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'star half solid'
  * @prop ...restProps
  */
 declare const StarHalfSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte
index b18a0ea..8b54ea2 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'star half stroke outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'star half stroke outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte.d.ts
index 3f9345b..7046e26 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'star half stroke outline'
  * @prop ...restProps
  */
 declare const StarHalfStrokeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte
index d9f64da..844e1b6 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'star half stroke solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'star half stroke solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte.d.ts
index 88639a5..11b1828 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StarHalfStrokeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'star half stroke solid'
  * @prop ...restProps
  */
 declare const StarHalfStrokeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte b/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte
index 0f1cc59..62f6c90 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'star outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'star outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte.d.ts
index 582e232..2c62e70 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StarOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'star outline'
  * @prop ...restProps
  */
 declare const StarOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte
index fae2900..02be8f5 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'star solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'star solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte.d.ts
index 190fd70..d60f538 100644
--- a/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StarSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'star solid'
  * @prop ...restProps
  */
 declare const StarSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte b/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte
index e1e8362..5b71816 100644
--- a/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'stop outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,9 +63,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'stop outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte.d.ts
index 301db77..ed4d464 100644
--- a/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StopOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'stop outline'
  * @prop ...restProps
  */
 declare const StopOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte
index 40de464..4380f1d 100644
--- a/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'stop solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -55,6 +54,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'stop solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte.d.ts
index 5dda45d..f0ba29e 100644
--- a/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StopSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'stop solid'
  * @prop ...restProps
  */
 declare const StopSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte b/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte
index d755c03..6da8e5e 100644
--- a/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'store outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'store outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte.d.ts
index 3f3c7d8..5f42efa 100644
--- a/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StoreOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'store outline'
  * @prop ...restProps
  */
 declare const StoreOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte
index 3ba5f10..9642ab9 100644
--- a/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'store solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'store solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte.d.ts
index 7d26e05..2c1094d 100644
--- a/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StoreSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'store solid'
  * @prop ...restProps
  */
 declare const StoreSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte b/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte
index d4c6c76..e3188ec 100644
--- a/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'stroopwafel outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'stroopwafel outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte.d.ts
index e0b9218..6060599 100644
--- a/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StroopwafelOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'stroopwafel outline'
  * @prop ...restProps
  */
 declare const StroopwafelOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte b/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte
index 6f20e6b..926bff8 100644
--- a/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'stroopwafel solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'stroopwafel solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte.d.ts
index bd1f04c..5a3f4c8 100644
--- a/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/StroopwafelSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'stroopwafel solid'
  * @prop ...restProps
  */
 declare const StroopwafelSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte
index 9b6b437..485c53d 100644
--- a/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'subscript outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'subscript outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte.d.ts
index a2f3987..d327bd6 100644
--- a/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SubscriptOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'subscript outline'
  * @prop ...restProps
  */
 declare const SubscriptOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte
index c1565bd..bb7cb7f 100644
--- a/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'sun outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'sun outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte.d.ts
index 16a505c..ed6e8ea 100644
--- a/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SunOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'sun outline'
  * @prop ...restProps
  */
 declare const SunOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte
index 46efcc0..a630cab 100644
--- a/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'sun solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'sun solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte.d.ts
index 194de8c..4a8caca 100644
--- a/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SunSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'sun solid'
  * @prop ...restProps
  */
 declare const SunSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte
index 522917d..125393e 100644
--- a/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'superscript outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'superscript outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte.d.ts
index a3712e4..ad62b26 100644
--- a/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SuperscriptOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'superscript outline'
  * @prop ...restProps
  */
 declare const SuperscriptOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte b/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte
index 0af0a88..9e070d4 100644
--- a/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'swatchbook outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'swatchbook outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte.d.ts
index 8512461..7ff67c4 100644
--- a/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SwatchbookOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'swatchbook outline'
  * @prop ...restProps
  */
 declare const SwatchbookOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte b/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte
index f0a39e6..e11818b 100644
--- a/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'swatchbook solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'swatchbook solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte.d.ts
index f610b28..6bdd698 100644
--- a/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/SwatchbookSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'swatchbook solid'
  * @prop ...restProps
  */
 declare const SwatchbookSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte
index 4293e6b..4a3ec3f 100644
--- a/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 't shirt outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 't shirt outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte.d.ts
index 4725fb3..fb7614e 100644
--- a/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TShirtOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 't shirt outline'
  * @prop ...restProps
  */
 declare const TShirtOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte
index 4f0800b..f202cc4 100644
--- a/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 't shirt solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 't shirt solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte.d.ts
index 9efc11c..305e7c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TShirtSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 't shirt solid'
  * @prop ...restProps
  */
 declare const TShirtSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte
index d58d4d2..a455681 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'table column outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'table column outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte.d.ts
index 0379a58..a50519e 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TableColumnOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'table column outline'
  * @prop ...restProps
  */
 declare const TableColumnOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte
index e867db9..0da413a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'table column solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'table column solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte.d.ts
index 16985c5..074a632 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TableColumnSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'table column solid'
  * @prop ...restProps
  */
 declare const TableColumnSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte
index 788df57..e3d098e 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'table row outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'table row outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte.d.ts
index 994d0e2..46258ea 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TableRowOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'table row outline'
  * @prop ...restProps
  */
 declare const TableRowOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte
index 591b325..6c6b215 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'table row solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'table row solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte.d.ts
index 9bb7091..cf6474d 100644
--- a/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TableRowSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'table row solid'
  * @prop ...restProps
  */
 declare const TableRowSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte
index a4ee986..3a5f925 100644
--- a/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tablet outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tablet outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte.d.ts
index 2af91ca..c18c68b 100644
--- a/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TabletOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tablet outline'
  * @prop ...restProps
  */
 declare const TabletOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte
index 2cfa959..6f5eda0 100644
--- a/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tablet solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tablet solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte.d.ts
index 8ea8887..12f46d1 100644
--- a/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TabletSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tablet solid'
  * @prop ...restProps
  */
 declare const TabletSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte
index 93aca86..6fa3365 100644
--- a/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tag outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tag outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte.d.ts
index 78d308e..2082ccf 100644
--- a/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TagOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tag outline'
  * @prop ...restProps
  */
 declare const TagOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte
index ca454f7..0c9ec6a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tag solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tag solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte.d.ts
index 9c67dca..fa08ea1 100644
--- a/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TagSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tag solid'
  * @prop ...restProps
  */
 declare const TagSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte
index 5d3bc62..dda2e0f 100644
--- a/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tailwind solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tailwind solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte.d.ts
index 4bfb94f..ae16aaf 100644
--- a/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TailwindSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tailwind solid'
  * @prop ...restProps
  */
 declare const TailwindSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte
index a3f4dab..dff1910 100644
--- a/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'teddy bear outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'teddy bear outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte.d.ts
index a5017d9..f82a0ec 100644
--- a/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TeddyBearOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'teddy bear outline'
  * @prop ...restProps
  */
 declare const TeddyBearOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte
index 222de39..9488240 100644
--- a/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'teddy bear solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'teddy bear solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte.d.ts
index 3c9b0ef..73a58f6 100644
--- a/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TeddyBearSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'teddy bear solid'
  * @prop ...restProps
  */
 declare const TeddyBearSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte
index 006f4ed..bb5f95f 100644
--- a/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'terminal outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'terminal outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte.d.ts
index e564057..280307a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TerminalOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'terminal outline'
  * @prop ...restProps
  */
 declare const TerminalOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte
index 5cc2d54..9fcfd64 100644
--- a/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'terminal solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'terminal solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte.d.ts
index 7680f08..15a7a6c 100644
--- a/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TerminalSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'terminal solid'
  * @prop ...restProps
  */
 declare const TerminalSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte
index 65da5e7..59a4ff5 100644
--- a/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'text size outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'text size outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte.d.ts
index d4837a1..b7ceabc 100644
--- a/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TextSizeOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'text size outline'
  * @prop ...restProps
  */
 declare const TextSizeOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte
index 914b7cc..8ef4065 100644
--- a/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'text slash outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'text slash outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte.d.ts
index 683adf7..e62fc9a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TextSlashOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'text slash outline'
  * @prop ...restProps
  */
 declare const TextSlashOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte
index e7455a8..01170fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'text underline outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'text underline outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte.d.ts
index b25283b..7bdc8a2 100644
--- a/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TextUnderlineOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'text underline outline'
  * @prop ...restProps
  */
 declare const TextUnderlineOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte
index c8d1dcd..25207f4 100644
--- a/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'theatre outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'theatre outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte.d.ts
index ff28cfb..5cc830b 100644
--- a/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TheatreOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'theatre outline'
  * @prop ...restProps
  */
 declare const TheatreOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte
index ce10b96..d8038d0 100644
--- a/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'theatre solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'theatre solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte.d.ts
index f064345..8bd437c 100644
--- a/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TheatreSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'theatre solid'
  * @prop ...restProps
  */
 declare const TheatreSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte
index 1fd0280..57fd628 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'thumbs down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'thumbs down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte.d.ts
index de14fa0..36bf23b 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'thumbs down outline'
  * @prop ...restProps
  */
 declare const ThumbsDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte
index 2bbd22c..7e2fc60 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'thumbs down solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'thumbs down solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte.d.ts
index 9d4841b..6bd3df4 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsDownSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'thumbs down solid'
  * @prop ...restProps
  */
 declare const ThumbsDownSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte
index ef39f62..9aac7fd 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'thumbs up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'thumbs up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte.d.ts
index 38d65bb..1c69b91 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'thumbs up outline'
  * @prop ...restProps
  */
 declare const ThumbsUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte
index 5eb3264..416f9c5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'thumbs up solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'thumbs up solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte.d.ts
index e1da81b..8411385 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbsUpSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'thumbs up solid'
  * @prop ...restProps
  */
 declare const ThumbsUpSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte
index 36b7c1a..58bd5c6 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'thumbtack outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'thumbtack outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte.d.ts
index ae74671..f48b6bc 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbtackOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'thumbtack outline'
  * @prop ...restProps
  */
 declare const ThumbtackOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte
index 5e631d3..e08a753 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'thumbtack solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'thumbtack solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte.d.ts
index 6bf37cd..6e472dd 100644
--- a/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ThumbtackSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'thumbtack solid'
  * @prop ...restProps
  */
 declare const ThumbtackSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte
index e35f5ba..08664e9 100644
--- a/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'ticket outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'ticket outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte.d.ts
index ed7ef25..4579775 100644
--- a/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TicketOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'ticket outline'
  * @prop ...restProps
  */
 declare const TicketOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte
index 4f662d1..9610402 100644
--- a/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'ticket solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'ticket solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte.d.ts
index 503eecb..4d766b8 100644
--- a/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TicketSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'ticket solid'
  * @prop ...restProps
  */
 declare const TicketSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte
index 38b12b3..e6a3da3 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'toggle header cell outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'toggle header cell outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte.d.ts
index 54f1514..0b8d5e2 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderCellOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'toggle header cell outline'
  * @prop ...restProps
  */
 declare const ToggleHeaderCellOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte
index 5e16ba8..6f98f2d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'toggle header column outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'toggle header column outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte.d.ts
index 585b024..46ebeea 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderColumnOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'toggle header column outline'
  * @prop ...restProps
  */
 declare const ToggleHeaderColumnOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte
index 108baa9..2607b92 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'toggle header row outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'toggle header row outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte.d.ts
index 1e14ce4..cc954aa 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ToggleHeaderRowOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'toggle header row outline'
  * @prop ...restProps
  */
 declare const ToggleHeaderRowOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte
index 08c1105..e100a84 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tools outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tools outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte.d.ts
index 7aca437..651fd1a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ToolsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tools outline'
  * @prop ...restProps
  */
 declare const ToolsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte
index 33a8ed3..7c3d131 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tracking outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tracking outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte.d.ts
index 3a21502..90674ff 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TrackingOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tracking outline'
  * @prop ...restProps
  */
 declare const TrackingOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte
index 7c89a91..2722318 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'tracking solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'tracking solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte.d.ts
index 8200172..375714d 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TrackingSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'tracking solid'
  * @prop ...restProps
  */
 declare const TrackingSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte
index 0f00b16..65a82c3 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'trash bin outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'trash bin outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte.d.ts
index dcf7232..6df248f 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TrashBinOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'trash bin outline'
  * @prop ...restProps
  */
 declare const TrashBinOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte
index 143d33e..275232a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'trash bin solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'trash bin solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte.d.ts
index e4c0eba..546f734 100644
--- a/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TrashBinSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'trash bin solid'
  * @prop ...restProps
  */
 declare const TrashBinSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte
index c19498d..61b240a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'truck clock outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'truck clock outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte.d.ts
index 9f1b73b..e859eee 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TruckClockOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'truck clock outline'
  * @prop ...restProps
  */
 declare const TruckClockOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte
index 03215d5..8c55b5a 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'truck clock solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'truck clock solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte.d.ts
index 99496af..a3e8437 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TruckClockSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'truck clock solid'
  * @prop ...restProps
  */
 declare const TruckClockSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte b/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte
index 305aa08..35bf745 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'truck outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'truck outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte.d.ts
index 828279c..fca7acb 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TruckOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'truck outline'
  * @prop ...restProps
  */
 declare const TruckOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte
index c1faebf..6b2ec18 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'truck solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'truck solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte.d.ts
index 57939a5..8589860 100644
--- a/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TruckSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'truck solid'
  * @prop ...restProps
  */
 declare const TruckSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte b/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte
index 13e2273..c3ee3a3 100644
--- a/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'twitter solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'twitter solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte.d.ts
index 8e4690e..c0e7553 100644
--- a/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/TwitterSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'twitter solid'
  * @prop ...restProps
  */
 declare const TwitterSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte
index 039b0df..b60e118 100644
--- a/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'undo outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'undo outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte.d.ts
index e8dcb65..8bdc8bd 100644
--- a/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UndoOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'undo outline'
  * @prop ...restProps
  */
 declare const UndoOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte
index d16a52e..1472afb 100644
--- a/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'upload outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'upload outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte.d.ts
index a4458a4..3be71bb 100644
--- a/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UploadOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'upload outline'
  * @prop ...restProps
  */
 declare const UploadOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte
index b534598..ef99455 100644
--- a/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'upload solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'upload solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte.d.ts
index 13ab117..57a8acd 100644
--- a/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UploadSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'upload solid'
  * @prop ...restProps
  */
 declare const UploadSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte
index f52ff38..dd15184 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user add outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user add outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte.d.ts
index f8065bf..2e14d97 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserAddOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user add outline'
  * @prop ...restProps
  */
 declare const UserAddOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte
index 4bb8feb..75272f7 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user add solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user add solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte.d.ts
index cbdbd47..16274ec 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserAddSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user add solid'
  * @prop ...restProps
  */
 declare const UserAddSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte
index 6892a04..625c690 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user circle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user circle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte.d.ts
index 7f8b0d2..d5ca0c5 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserCircleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user circle outline'
  * @prop ...restProps
  */
 declare const UserCircleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte
index ff13361..2201e43 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user circle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user circle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte.d.ts
index 4c341af..afe2ea1 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserCircleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user circle solid'
  * @prop ...restProps
  */
 declare const UserCircleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte
index a0638ac..9e8ff67 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user edit outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user edit outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte.d.ts
index b0e531c..7a3aff5 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserEditOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user edit outline'
  * @prop ...restProps
  */
 declare const UserEditOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte
index ef97a5b..5248931 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user edit solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user edit solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte.d.ts
index 8977eef..3d285a5 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserEditSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user edit solid'
  * @prop ...restProps
  */
 declare const UserEditSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte
index eafbe4e..726e0a9 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user graduate outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user graduate outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte.d.ts
index 9902f91..6cb3071 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserGraduateOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user graduate outline'
  * @prop ...restProps
  */
 declare const UserGraduateOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte
index 890120c..fa03933 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user graduate solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user graduate solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte.d.ts
index 15bd8ac..80b507a 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserGraduateSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user graduate solid'
  * @prop ...restProps
  */
 declare const UserGraduateSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte
index 8d3be05..5daa760 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user headset outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user headset outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte.d.ts
index de53036..3af4aca 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserHeadsetOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user headset outline'
  * @prop ...restProps
  */
 declare const UserHeadsetOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte
index 6a00408..2cb5d9e 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user headset solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user headset solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte.d.ts
index 4d13cd3..5ba0a25 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserHeadsetSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user headset solid'
  * @prop ...restProps
  */
 declare const UserHeadsetSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte
index 350d8ec..4aa38c5 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,9 +58,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte.d.ts
index c2f3348..b0561f8 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user outline'
  * @prop ...restProps
  */
 declare const UserOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte
index b9eb016..04e777f 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user remove outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user remove outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte.d.ts
index a17d9e2..21a48fe 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserRemoveOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user remove outline'
  * @prop ...restProps
  */
 declare const UserRemoveOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte
index e3035d2..fde6b7f 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user remove solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user remove solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte.d.ts
index 1c7cace..f58bd50 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserRemoveSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user remove solid'
  * @prop ...restProps
  */
 declare const UserRemoveSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte
index e2204a1..fd39ee7 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user settings outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user settings outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte.d.ts
index 02467a9..97615d2 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserSettingsOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user settings outline'
  * @prop ...restProps
  */
 declare const UserSettingsOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte
index 854b9ae..c8c8ab2 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user settings solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user settings solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte.d.ts
index becb79e..7777e87 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserSettingsSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user settings solid'
  * @prop ...restProps
  */
 declare const UserSettingsSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte
index 8268660..6b0d04d 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'user solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'user solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte.d.ts
index 5ebdbb3..9f3d736 100644
--- a/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UserSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'user solid'
  * @prop ...restProps
  */
 declare const UserSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte
index 47d6fd0..b72b3c5 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'users group outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'users group outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte.d.ts
index 9588a83..4abcdeb 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UsersGroupOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'users group outline'
  * @prop ...restProps
  */
 declare const UsersGroupOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte
index 8b06dfa..5db1465 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'users group solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'users group solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte.d.ts
index 8657d74..d6441fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UsersGroupSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'users group solid'
  * @prop ...restProps
  */
 declare const UsersGroupSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte b/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte
index d74df1b..b685ec8 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'users outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'users outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte.d.ts
index 19fe9f0..8620fca 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UsersOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'users outline'
  * @prop ...restProps
  */
 declare const UsersOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte b/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte
index 4c81f75..7c4c19b 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'users solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'users solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte.d.ts
index 95bf780..ac48c1f 100644
--- a/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/UsersSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'users solid'
  * @prop ...restProps
  */
 declare const UsersSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte b/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte
index c8e706b..0e3e28b 100644
--- a/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'video camera outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'video camera outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte.d.ts
index bd537ef..b02e052 100644
--- a/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VideoCameraOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'video camera outline'
  * @prop ...restProps
  */
 declare const VideoCameraOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte b/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte
index 16212fc..dd35130 100644
--- a/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'video camera solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'video camera solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte.d.ts
index 80b01c0..db08f43 100644
--- a/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VideoCameraSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'video camera solid'
  * @prop ...restProps
  */
 declare const VideoCameraSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte b/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte
index 02062f3..fd62a7a 100644
--- a/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'visa solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,6 +59,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'visa solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte.d.ts
index a52fe97..dbe443c 100644
--- a/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VisaSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'visa solid'
  * @prop ...restProps
  */
 declare const VisaSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte b/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte
index 67456c8..6c3f392 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'volume down outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'volume down outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte.d.ts
index 57334a4..c9e3d57 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeDownOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'volume down outline'
  * @prop ...restProps
  */
 declare const VolumeDownOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte b/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte
index d413b42..3759d4f 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'volume down solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'volume down solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte.d.ts
index 112e1aa..f40fbbc 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeDownSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'volume down solid'
  * @prop ...restProps
  */
 declare const VolumeDownSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte b/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte
index 6fb39dc..fddb823 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'volume mute outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'volume mute outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte.d.ts
index a1a442d..786fa25 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeMuteOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'volume mute outline'
  * @prop ...restProps
  */
 declare const VolumeMuteOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte b/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte
index f0bd365..df3bdef 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'volume mute solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'volume mute solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte.d.ts
index 0a2986b..04c16a1 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeMuteSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'volume mute solid'
  * @prop ...restProps
  */
 declare const VolumeMuteSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte b/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte
index de7c064..6c4ff7a 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'volume up outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'volume up outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte.d.ts
index 650aa82..19691ab 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeUpOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'volume up outline'
  * @prop ...restProps
  */
 declare const VolumeUpOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte b/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte
index 85f8eb4..adc38f8 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'volume up solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -67,6 +66,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'volume up solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte.d.ts
index 42937b7..3fc3867 100644
--- a/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VolumeUpSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'volume up solid'
  * @prop ...restProps
  */
 declare const VolumeUpSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte b/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte
index 108890f..f959bcb 100644
--- a/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'vue solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'vue solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte.d.ts
index 140753c..4e13fb7 100644
--- a/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/VueSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'vue solid'
  * @prop ...restProps
  */
 declare const VueSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte
index f016f4b..2b38339 100644
--- a/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wallet outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wallet outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte.d.ts
index 27c11c9..c57713b 100644
--- a/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WalletOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wallet outline'
  * @prop ...restProps
  */
 declare const WalletOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte
index 97b33a4..e02ca82 100644
--- a/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wallet solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wallet solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte.d.ts
index b633744..a8171ac 100644
--- a/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WalletSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wallet solid'
  * @prop ...restProps
  */
 declare const WalletSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte
index f7bc4f0..f9ead9c 100644
--- a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wand magic sparkles outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wand magic sparkles outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte.d.ts
index 8f9bc3c..d83ee2a 100644
--- a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wand magic sparkles outline'
  * @prop ...restProps
  */
 declare const WandMagicSparklesOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte
index 305031c..a17ce8b 100644
--- a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wand magic sparkles solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wand magic sparkles solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte.d.ts
index ab6e09e..c43acf3 100644
--- a/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WandMagicSparklesSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wand magic sparkles solid'
  * @prop ...restProps
  */
 declare const WandMagicSparklesSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte
index dda0a7f..ae21751 100644
--- a/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'water bottle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'water bottle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte.d.ts
index af67ef3..7b4eed7 100644
--- a/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WaterBottleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'water bottle outline'
  * @prop ...restProps
  */
 declare const WaterBottleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte
index a65aedf..f16018d 100644
--- a/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'water bottle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'water bottle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte.d.ts
index 4c5aa4f..def52f2 100644
--- a/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WaterBottleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'water bottle solid'
  * @prop ...restProps
  */
 declare const WaterBottleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte
index bd9668e..dac54ca 100644
--- a/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'whatsapp solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -62,6 +61,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'whatsapp solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte.d.ts
index 3889948..ce87754 100644
--- a/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WhatsappSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'whatsapp solid'
  * @prop ...restProps
  */
 declare const WhatsappSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte
index 84d54e1..521b233 100644
--- a/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wheat exclamation outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wheat exclamation outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte.d.ts
index c79617f..bce4455 100644
--- a/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WheatExclamationOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wheat exclamation outline'
  * @prop ...restProps
  */
 declare const WheatExclamationOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte
index 9670d99..bc543ab 100644
--- a/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wheat outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wheat outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte.d.ts
index 41a0d28..f7b6bd8 100644
--- a/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WheatOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wheat outline'
  * @prop ...restProps
  */
 declare const WheatOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte
index 2dd12c7..637872c 100644
--- a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'whiskey glass outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'whiskey glass outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte.d.ts
index 2a04ae7..88ba712 100644
--- a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'whiskey glass outline'
  * @prop ...restProps
  */
 declare const WhiskeyGlassOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte
index 375661d..297d43a 100644
--- a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'whiskey glass solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'whiskey glass solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte.d.ts
index f0e8e03..19a9c4a 100644
--- a/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WhiskeyGlassSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'whiskey glass solid'
  * @prop ...restProps
  */
 declare const WhiskeyGlassSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte
index cbccc17..d6656a3 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'window outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'window outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte.d.ts
index 6288776..8495dca 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WindowOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'window outline'
  * @prop ...restProps
  */
 declare const WindowOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte
index be8437c..efac4c6 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'window restore solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -64,6 +63,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'window restore solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte.d.ts
index 84148b4..36612dd 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WindowRestoreSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'window restore solid'
  * @prop ...restProps
  */
 declare const WindowRestoreSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte
index 0ac54dc..d366942 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'window solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'window solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte.d.ts
index 98e2814..229697c 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WindowSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'window solid'
  * @prop ...restProps
  */
 declare const WindowSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte
index af2a4e0..3e76947 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'windows solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'windows solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte.d.ts
index f74d53c..5fa0a28 100644
--- a/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WindowsSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'windows solid'
  * @prop ...restProps
  */
 declare const WindowsSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte
index a5bfc90..02da266 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wine bottle outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wine bottle outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte.d.ts
index 67ce3a5..6b4f102 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WineBottleOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wine bottle outline'
  * @prop ...restProps
  */
 declare const WineBottleOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte
index 5fbeb1c..b38788d 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wine bottle solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wine bottle solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte.d.ts
index 71ff5ea..bfae585 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WineBottleSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wine bottle solid'
  * @prop ...restProps
  */
 declare const WineBottleSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte
index 7193d26..8cede96 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wine glass empty outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wine glass empty outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte.d.ts
index 9f1e9c8..7b88bfb 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptyOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wine glass empty outline'
  * @prop ...restProps
  */
 declare const WineGlassEmptyOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte
index 3ec6cd2..e12c1b8 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wine glass empty solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wine glass empty solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte.d.ts
index ff716d9..97b812f 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassEmptySolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wine glass empty solid'
  * @prop ...restProps
  */
 declare const WineGlassEmptySolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte b/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte
index e29b56c..31c3dd0 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wine glass outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -61,9 +60,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wine glass outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte.d.ts
index 2c1c7c9..beb8271 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wine glass outline'
  * @prop ...restProps
  */
 declare const WineGlassOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte b/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte
index 6b65c2c..38547fa 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'wine glass solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'wine glass solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte.d.ts
index 6ff947f..0aaf9b4 100644
--- a/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/WineGlassSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'wine glass solid'
  * @prop ...restProps
  */
 declare const WineGlassSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/XSolid.svelte b/node_modules/flowbite-svelte-icons/dist/XSolid.svelte
index 67338f3..e0e7951 100644
--- a/node_modules/flowbite-svelte-icons/dist/XSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/XSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'X solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -57,6 +56,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'X solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/XSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/XSolid.svelte.d.ts
index bb2e501..35ce9c0 100644
--- a/node_modules/flowbite-svelte-icons/dist/XSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/XSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'X solid'
  * @prop ...restProps
  */
 declare const XSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte b/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte
index c0e227e..f35f28d 100644
--- a/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'youtube solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'youtube solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte.d.ts
index 2595b89..7d932a9 100644
--- a/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/YoutubeSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'youtube solid'
  * @prop ...restProps
  */
 declare const YoutubeSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte
index 4d43a6f..e9c491a 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'zoom in outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'zoom in outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte.d.ts
index bbd69d1..b2c5bd0 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomInOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'zoom in outline'
  * @prop ...restProps
  */
 declare const ZoomInOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte
index 82c3742..5f1834d 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'zoom in solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'zoom in solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte.d.ts
index 0652f6d..6648521 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomInSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'zoom in solid'
  * @prop ...restProps
  */
 declare const ZoomInSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte b/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte
index abf9196..424cf02 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { OutlineBaseProps, OutlineProps } from './types';
 
 	const ctx: OutlineBaseProps = getContext('iconCtx') ?? {};
@@ -16,16 +17,15 @@
 		size = ctx.size || 'md',
 		color = ctx.color || 'currentColor',
 		title,
-		strokeWidth = ctx.strokeWidth || 2,
+		strokeWidth = ctx.strokeWidth || '2',
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'zoom out outline',
 		...restProps
 	}: OutlineProps = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
@@ -33,11 +33,10 @@
 	fill="none"
 	{color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -60,9 +59,9 @@
 @prop size = ctx.size || 'md'
 @prop color = ctx.color || 'currentColor'
 @prop title
-@prop strokeWidth = ctx.strokeWidth || 2
+@prop strokeWidth = ctx.strokeWidth || '2'
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'zoom out outline'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte.d.ts
index 4a76187..2d69f43 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomOutOutline.svelte.d.ts
@@ -5,10 +5,10 @@ import type { OutlineProps } from './types';
  * @prop size = ctx.size || 'md'
  * @prop color = ctx.color || 'currentColor'
  * @prop title
- * @prop strokeWidth = ctx.strokeWidth || 2
+ * @prop strokeWidth = ctx.strokeWidth || '2'
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'zoom out outline'
  * @prop ...restProps
  */
 declare const ZoomOutOutline: import("svelte").Component<OutlineProps, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte b/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte
index 6e4d494..0e69df5 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte
@@ -1,6 +1,7 @@
 <script lang="ts">
 	import { getContext } from 'svelte';
-	import { cn } from './helpers';
+	import { twMerge } from 'tailwind-merge';
+	import clsx from 'clsx';
 	import type { BaseProps, Props } from './types';
 
 	const ctx: BaseProps = getContext('iconCtx') ?? {};
@@ -18,24 +19,22 @@
 		title,
 		desc,
 		class: className,
-		ariaLabel,
+		ariaLabel = 'zoom out solid',
 		...restProps
 	}: Props = $props();
 
-	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`.trim();
-	const hasDescription = !!(title?.id || desc?.id);
-	const isLabeled = !!ariaLabel || hasDescription;
+	let ariaDescribedby = `${title?.id || ''} ${desc?.id || ''}`;
+	const hasDescription = $derived(!!(title?.id || desc?.id));
 </script>
 
 <svg
 	xmlns="http://www.w3.org/2000/svg"
 	fill={color}
 	{...restProps}
-	class={cn('shrink-0', sizes[size], className)}
-	viewBox="0 0 24 24"
+	class={twMerge(clsx('shrink-0', sizes[size], className))}
 	aria-label={ariaLabel}
 	aria-describedby={hasDescription ? ariaDescribedby : undefined}
-	aria-hidden={!isLabeled}
+	viewBox="0 0 24 24"
 >
 	{#if title?.id && title.title}
 		<title id={title.id}>{title.title}</title>
@@ -59,6 +58,6 @@
 @prop title
 @prop desc
 @prop class: className
-@prop ariaLabel
+@prop ariaLabel = 'zoom out solid'
 @prop ...restProps
 -->
diff --git a/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte.d.ts b/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte.d.ts
index a3d9468..fe18b96 100644
--- a/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/ZoomOutSolid.svelte.d.ts
@@ -7,7 +7,7 @@ import type { Props } from './types';
  * @prop title
  * @prop desc
  * @prop class: className
- * @prop ariaLabel
+ * @prop ariaLabel = 'zoom out solid'
  * @prop ...restProps
  */
 declare const ZoomOutSolid: import("svelte").Component<Props, {}, "">;
diff --git a/node_modules/flowbite-svelte-icons/dist/helpers.d.ts b/node_modules/flowbite-svelte-icons/dist/helpers.d.ts
deleted file mode 100644
index 19c0e0f..0000000
--- a/node_modules/flowbite-svelte-icons/dist/helpers.d.ts
+++ /dev/null
@@ -1,2 +0,0 @@
-import { type ClassValue } from 'clsx';
-export declare function cn(...inputs: ClassValue[]): string;
diff --git a/node_modules/flowbite-svelte-icons/dist/helpers.js b/node_modules/flowbite-svelte-icons/dist/helpers.js
deleted file mode 100644
index c831202..0000000
--- a/node_modules/flowbite-svelte-icons/dist/helpers.js
+++ /dev/null
@@ -1,5 +0,0 @@
-import { clsx } from 'clsx';
-import { twMerge } from 'tailwind-merge';
-export function cn(...inputs) {
-    return twMerge(clsx(inputs));
-}
diff --git a/node_modules/flowbite-svelte-icons/dist/package.json b/node_modules/flowbite-svelte-icons/dist/package.json
index 7f9d106..4d66b41 100644
--- a/node_modules/flowbite-svelte-icons/dist/package.json
+++ b/node_modules/flowbite-svelte-icons/dist/package.json
@@ -1,6 +1,6 @@
 {
   "name": "flowbite-svelte-icons",
-  "version": "2.3.0",
+  "version": "2.2.1",
   "description": "Flowbite icon components for Svelte 5 Runes",
   "author": {
     "name": "Shinichi Okada",
@@ -67,43 +67,43 @@
     "svelte": "^5.0.0"
   },
   "devDependencies": {
-    "@changesets/cli": "^2.29.5",
-    "@eslint/compat": "^1.3.1",
-    "@eslint/js": "^9.31.0",
-    "@playwright/test": "^1.54.1",
-    "@sveltejs/adapter-auto": "^6.1.0",
-    "@sveltejs/kit": "^2.25.1",
-    "@sveltejs/package": "^2.4.0",
-    "@sveltejs/vite-plugin-svelte": "^6.1.0",
+    "@changesets/cli": "^2.29.4",
+    "@eslint/compat": "^1.3.0",
+    "@eslint/js": "^9.29.0",
+    "@playwright/test": "^1.53.0",
+    "@sveltejs/adapter-auto": "^4.0.0",
+    "@sveltejs/kit": "^2.21.5",
+    "@sveltejs/package": "^2.3.11",
+    "@sveltejs/vite-plugin-svelte": "^5.1.0",
     "@svitejs/changesets-changelog-github-compact": "^1.2.0",
-    "@tailwindcss/vite": "^4.1.11",
+    "@tailwindcss/vite": "^4.1.10",
     "@testing-library/jest-dom": "^6.6.3",
     "@testing-library/svelte": "^5.2.8",
-    "eslint": "^9.31.0",
-    "eslint-config-prettier": "^10.1.8",
-    "eslint-plugin-svelte": "^3.11.0",
-    "flowbite": "^3.1.2",
-    "flowbite-svelte": "^1.12.5",
-    "globals": "^16.3.0",
+    "eslint": "^9.29.0",
+    "eslint-config-prettier": "^10.1.5",
+    "eslint-plugin-svelte": "^3.9.2",
+    "globals": "^16.2.0",
     "highlight.js": "^11.11.1",
     "jsdom": "^26.1.0",
-    "prettier": "^3.6.2",
+    "prettier": "^3.5.3",
     "prettier-plugin-svelte": "^3.4.0",
-    "prettier-plugin-tailwindcss": "^0.6.14",
+    "prettier-plugin-tailwindcss": "^0.6.12",
     "publint": "^0.3.12",
     "runatics": "^0.1.4",
-    "runes-meta-tags": "^0.4.4",
-    "runes-webkit": "^0.11.1",
-    "svelte": "^5.36.12",
-    "svelte-check": "^4.3.0",
+    "runes-meta-tags": "^0.4.3",
+    "runes-webkit": "^0.9.59",
+    "svelte": "^5.34.3",
+    "svelte-5-ui-lib": "^0.12.6",
+    "svelte-check": "^4.2.1",
     "svelte-lib-helpers": "^0.4.30",
-    "svelte-rune-highlight": "^0.7.1",
+    "svelte-rune-highlight": "^0.5.15",
     "svelte-shields": "^0.5.1",
-    "tailwindcss": "^4.1.11",
+    "tailwind-merge": "^3.3.0",
+    "tailwindcss": "^4.1.10",
     "typescript": "^5.8.3",
-    "typescript-eslint": "^8.37.0",
+    "typescript-eslint": "^8.34.0",
     "vite": "^6.3.5",
-    "vitest": "^3.2.4"
+    "vitest": "^3.2.3"
   },
   "pnpm": {
     "onlyBuiltDependencies": [
diff --git a/node_modules/flowbite-svelte-icons/dist/types.d.ts b/node_modules/flowbite-svelte-icons/dist/types.d.ts
index 0734c12..16bb6e8 100644
--- a/node_modules/flowbite-svelte-icons/dist/types.d.ts
+++ b/node_modules/flowbite-svelte-icons/dist/types.d.ts
@@ -1,26 +1,30 @@
 import type { SVGAttributes } from 'svelte/elements';
-export type Size = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
+import type { ClassValue } from 'clsx';
 export type TitleType = {
-    id: string;
+    id?: string;
     title?: string;
-} | undefined;
+};
 export type DescType = {
-    id: string;
+    id?: string;
     desc?: string;
-} | undefined;
-export interface BaseProps extends SVGAttributes<SVGElement> {
-    size?: Size;
-    color?: string | null;
+};
+export interface BaseProps extends Omit<SVGAttributes<SVGElement>, 'class'> {
+    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
+    color?: string | undefined | null;
+    class?: ClassValue;
 }
 export interface OutlineBaseProps extends BaseProps {
-    strokeWidth?: number | `${number}`;
+    strokeWidth?: string | undefined | null;
 }
-export interface AccessibleProps {
+export interface Props extends BaseProps {
     title?: TitleType;
     desc?: DescType;
     ariaLabel?: string;
+    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
 }
-export interface Props extends BaseProps, AccessibleProps {
-}
-export interface OutlineProps extends OutlineBaseProps, AccessibleProps {
+export interface OutlineProps extends OutlineBaseProps {
+    title?: TitleType;
+    desc?: DescType;
+    ariaLabel?: string;
+    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
 }
